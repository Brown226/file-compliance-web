# 未提交代码变更分析报告

> 生成时间：2026-08-07 16:10（GMT+8）
> 分支：`v2.0`（与 `origin/v2.0` 同步，无落后/超前）
> 规模：**25 个已修改文件 + 5 个新增测试文件**，净增 `+682 / -146`
> 状态：本报告仅分析，**未执行任何 git 提交/暂存操作**

---

## 0. 一图流：改动主题地图

```
未提交改动（6 大主题 + 测试补齐）
│
├─ A. V3.2 条文库合并（审点迁移重构）────── 最大、最核心
├─ B. 智能判标层 + P1-6（LOW 不再丢弃）
├─ C. P1-5 结构化一致性健壮性（隔离解析）
├─ D. P1-8 规则引擎粒度修复（前缀/severity）
├─ E. P1-7 空库显式告警（区分"没审到"）
├─ F. 分片 CRLF 归一化（Windows 文档修复）
└─ G. 测试补齐（5 新增 + 3 修改）
```

> **关键交叠**：`review.service.ts` 一个文件同时承载了 **A（审点迁移）+ B（判标落库）+ E（空库告警）** 三件事；
> `schema.prisma` 同时承载了 **A（standardId）+ B（judgeConfidence）**；`llm.service.ts` 的 `ReviewIssue` 类型是 **B 与 C/D** 的共享契约。

---

## A. V3.2 条文库合并（审点迁移重构）

**目标**：把审点的存储载体从独立的 `StandardCheckpoint` 表统一合并到 `rule_library_items`（语义规则库条目），标准通过 `rule_libraries.standardId` 反向关联审点库。审计点（clauseText/checkPrompt/auditDimension/mandatory）现在只有 `rule_library_items` 一个真相源。

**改动文件**：
| 文件 | 改动要点 |
|------|----------|
| `backend/prisma/schema.prisma` | `RuleLibrary` 加 `standardId`（可选，FK→Standard，`onDelete: SetNull`）+ `@@index([standardId])`；`Standard` 加反向关系 `ruleLibraries` |
| `backend/src/services/standard/checkpoint/checkpoint.service.ts` | **整体重写**：`listByStandard/getById/update/delete/getStats` 全部转查 `rule_library_items`，对外返回形状用 `adaptItem()` 兼容旧 `StandardCheckpoint` 形状 |
| `backend/src/services/agent/tools/knowledge/search_standard_checkpoints.ts` | 工具重写：列出标准模式改为"仅列已关联审点库且库内有审点的标准"；查审点模式经 `rule_libraries.standardId → rule_library_items`；返回结构新增 `libraryId` |
| `backend/src/services/review/review.service.ts` | DEC/LIBRARY_REVIEW 预加载由"双源（StandardCheckpoint + RuleLibraryItem）"改为"单源（rule_library_items）"；按 `libraryIds` 集合去重，避免标准关联库与直挂库重复 |
| `backend/src/services/llm/rule-library.service.ts` | `list()` 支持 `standardId` 过滤；`create()` 支持写入 `standardId` |
| `backend/src/controllers/rule-library.controller.ts` | list/create 接口透传 `standardId` |
| `backend/src/routes/checkpoint.routes.ts` | 审点生成接口把 `createdBy`（当前用户 id）传给 `extractAndSave` |
| `backend/src/services/standard/checkpoint/checkpoint-extractor.service.ts` | 配合接收 `createdBy` 参数（51 行改动） |
| `frontend/src/api/rule-library.ts` | `RuleLibrary` 类型加 `standardId`；list/create 接口参数对齐 |

**风险点**：
1. **`StandardCheckpoint` 模型现为死模型**：全代码已无任何 `prisma.standardCheckpoint` 查询（已确认），仅 `review-pipeline/types.ts:180` 残留一处过时注释「StandardCheckpoint 记录」。schema 仍定义该模型 + `Standard.standardCheckpoint` 关系，但无人读写。→ **待确认是否清理**（留着无害但累积死 schema；删需同步改 types.ts 注释）。
2. **`onDelete: SetNull`**：删除标准时审点库不删，仅 `standardId` 置空。符合"保留标准树组织"的设计意图，但需注意：置空后该审点库在 `search_standard_checkpoints` 的"列出标准模式"中将不再出现。
3. **容器运行的是 `dist`，不是 src**（AGENTS.md 已记录）：改完必须 `npm run build` 并清空旧 `dist`，否则线上"改了没生效"。

---

## B. 智能判标层 + P1-6（LOW 不再丢弃）

**目标**：为审查问题引入"判标置信度"（`HIGH/MEDIUM/LOW`），并把 **LOW 置信度从"直接丢弃"改为"保留并标记待人工复核"**，避免疑似误报从用户视野消失（P1-6）。

**改动文件**：
| 文件 | 改动要点 |
|------|----------|
| `backend/src/services/llm/llm.service.ts` | `ReviewIssue` 接口新增 `confidence?: 'HIGH'\|'MEDIUM'\|'LOW'` 与 `confidenceReason?: string`（共享契约） |
| `backend/src/services/review/cross-review/smart-judge.service.ts` | **P1-6**：移除"过滤丢弃 LOW"逻辑，改为保留全部并标注；写回 `issue.confidence`/`issue.confidenceReason`（类型化，不再 `any` 硬挂） |
| `backend/src/services/review/review.service.ts` | 落库 `TaskDetail` 时写入 `judgeConfidence`/`judgeReason`；`reviewStatus` 联动规则扩展为 `AI_INFERRED \|\| riskLevel==='HIGH' \|\| confidence==='LOW'` → `PENDING_REVIEW` |
| `backend/prisma/schema.prisma` | `TaskDetail` 新增 `judgeConfidence`（`judge_confidence`）、`judgeReason`（`@db.Text`） |
| `frontend/src/types/models.d.ts` | `TaskDetail` 前端类型加 `judgeConfidence`/`judgeReason` |
| `frontend/src/views/TaskDetails/IssueCard.vue` | 新增「待复核」徽标（`PENDING_REVIEW` 时）+ 「判标 HIGH/MEDIUM」徽标（LOW 由待复核徽标承载，避免双标签）；`title` 显示 `judgeReason` |
| `frontend/src/views/TaskResultsView.vue` | 明细拉取时把 `judgeConfidence`/`judgeReason` 映射进卡片数据 |

**风险点**：
- 链路完整（LLM→smart-judge→review 落库→前端展示），但需确认 **前端展示字段 `d.judgeConfidence` 的默认值处理**：`fetchData` 里用了 `d.judgeConfidence || null`，后端若返回 `undefined` 会落成 `null`，与 schema 默认一致，OK。
- P1-6 变更会**改变历史问题的呈现**：此前被丢弃的 LOW 问题现在会作为"待复核"出现。对历史已审任务无影响（不回刷），仅影响新任务。

---

## C. P1-5 结构化一致性健壮性（隔离解析）

**目标**：修复结构化一致性比对（CONSISTENCY 模式）中 **LLM 输出畸形导致整片抽取/比对结果被静默丢弃** 的问题（单分类崩溃吞整片、单条畸形吞整片、压缩时丢 meta/facts 导致 C5/C6 漏报）。

**改动文件**：`backend/src/services/review/structured-consistency.service.ts`（+167/-… 本次最大单文件改动）
- 抽取解析：原"一次性 map/filter 5 类"改为 **逐分类独立 `parseExtractParams/Codes/Refs/Meta/Facts`**，每个带独立 `try/catch` + `safeArray()` 兜底（非数组类型统一降级为 `[]`）。单分类畸形只丢该分类，其余 4 类保留。
- 压缩（`compressMergedSummary`）：**保留全部 5 维度**（之前完全丢弃 meta/facts，导致 C5/C6 静默漏报），预算按比例分配 `params 40% / codes 25% / refs 15% / meta 10% / facts 10%`。
- 比对结果解析：原"数组 map 一次性转 issue"改为 **逐条 `try/catch`**，单条畸形/null 只丢该条；`issueType` 校验白名单（13 类），非法值回退 `CONSISTENCY`。

**风险点**：日志量会增加（`console.warn` 每类/每条异常都打）。属预期内的可观测性提升。

---

## D. P1-8 规则引擎粒度修复（前缀/severity）

**目标**：修复规则引擎按 `ruleCode` 反查注册表前缀时的**断链**（`CONTRACT_PAYMENT_001` 被旧逻辑 `replace(/_\d+$/,'')` 误提为 `CONTRACT_PAYMENT`，而注册表前缀是 `CONTRACT`，导致 DB 配置永远查不到），以及 severity 覆盖粒度丢失。

**改动文件**：
| 文件 | 改动要点 |
|------|----------|
| `backend/src/services/rules/index.ts` | 新增 `matchRulePrefix()`（**最长前缀优先**匹配，如 `DWG_TITLE` 先于 `DWG`）；`loadRuleConfigsFromDB()` 双 key 保存——完整 `ruleCode`（保留 issue 级粒度，如 `CONTRACT_PAYMENT_001`）+ 注册表前缀聚合（用于规则级 enabled）；新增 `mergeSeverityMaps()` 合并 DB 配置与调用方 `options.severityMap`（修复旧实现"DB 存在时直接丢弃 options"） |
| `backend/src/services/rules/header.rule.ts` | 配套调整（18 行） |
| `backend/src/services/rules/page.rule.ts` | 配套调整（31 行） |
| 测试 | `header.rule.test.ts`(+44)、`page.rule.test.ts`(+40)、新增 `rule-engine-mechanism.test.ts`（机制级测试） |

**风险点**：
- 双 key（完整 ruleCode + 前缀）会让 `configMap` 体积翻倍，但规模很小（规则数有限），无性能问题。
- 前缀聚合逻辑改为"只更新 enabled，不覆盖 severity/config"——因为粒度已由完整 ruleCode 保留。需确认前端"规则配置面板"展示的是哪一层 key，避免 UI 显示与引擎实际行为不一致。

---

## E. P1-7 空库显式告警（区分"没审到"）

**目标**：当任务关联的标准库/审点库为空时，明确告诉用户"本次未发现问题可能是没审到，而非真合规"，通过 WebSocket 实时告警 + 落库 `stats.warnings` 双重保障（轮询/刷新兜底）。

**改动文件**：
| 文件 | 改动要点 |
|------|----------|
| `backend/src/services/review/review.service.ts` | DEC_REVIEW 空库分支：原仅 `console.warn` → 新增 `WebSocketService.emitTaskProgress({type:'lib_empty',...})` + 写 `task.stats.warnings` 落库 |
| `backend/src/services/review/self-check.service.ts` | `SelfCheckReport` 加 `warning?` 字段；标准库为空时生成告警文案 |
| `backend/src/controllers/self-check.controller.ts` | 自检完成后若 `report.warning` 非空，同样发 WebSocket + 落库 `stats.warnings` |
| `frontend/src/views/TaskResultsView.vue` | 新增"审查数据缺失"告警横幅（`libEmptyWarnings`），从 WebSocket `lib_empty` 消息或 `stats.warnings` 恢复；含"未发现问题≠真合规"提示文案 |

**风险点**：
- 两处空库逻辑（review.service 的 DEC 分支 + self-check.controller）各自独立写 `stats.warnings`，存在**并发覆盖**可能（都在 `task.stats` 上 read-modify-write）。当前是任务级串行，风险低，但多文件/多阶段并行时需留意。
- 前端横幅 `:closable` 关闭后仅前端内存清除，刷新页面会从 `stats.warnings` 重新拉起——符合"兜底可见"设计。

---

## F. 分片 CRLF 归一化（Windows 文档修复）

**目标**：修复 Windows（CRLF）文档在分片/大纲提取时因残留 `\r` 导致的 **标题正则匹配失效、字符偏移错位、超长章节不二次切段** 问题。

**改动文件**：`backend/src/services/review/chunk-splitter.service.ts`
- `extractOutline` / `splitBySection` / `split` 三处统一 `text.replace(/\r\n/g, '\n')` 归一化。
- 段落切分正则由 `/\n\n+/` 改为 `/\r?\n\r?\n+/`，兼容 CRLF 段落分隔。

**风险点**：纯防御性归一化，对 LF 文档无副作用。`maxChunkSize` 单位现为 LF 字符数，与归一化后一致，偏移量计算正确。

---

## G. 测试补齐

**新增（5 个文件）**：
- `backend/src/services/review/__tests__/chunk-splitter.service.test.ts`（对应主题 F）
- `backend/src/services/review/__tests__/self-check-empty-lib.test.ts`（对应主题 E）
- `backend/src/services/review/__tests__/structured-consistency.test.ts`（对应主题 C）
- `backend/src/services/review/cross-review/__tests__/smart-judge.test.ts`（对应主题 B/P1-6）
- `backend/src/services/rules/__tests__/rule-engine-mechanism.test.ts`（对应主题 D）

**修改（3 个文件）**：
- `backend/src/services/review-pipeline/__tests__/doc-review-agent.test.ts`(+13)
- `backend/src/services/rules/__tests__/header.rule.test.ts`(+44)
- `backend/src/services/rules/__tests__/page.rule.test.ts`(+40)

> 全部位于 `backend/src/**/__tests__/`，符合项目"全 mock 隔离、无 DB/Redis/LLM 依赖"的测试约定。建议提交前跑 `cd backend && npm test` 确认 553→新增用例全绿。

---

## 跨主题交叠清单（提交拆分时需注意）

| 交叠文件 | 承载的主题 | 拆分建议 |
|----------|-----------|----------|
| `backend/src/services/review/review.service.ts` | A + B + E | 若按主题拆 commit，此文件需按 hunks 切分，或整体随"主主题 A"提交并在 message 中说明附带 B/E |
| `backend/prisma/schema.prisma` | A + B | 同上，DB 迁移必须整体生效，建议 schema 改动与首次依赖它的服务同 commit |
| `backend/src/services/llm/llm.service.ts` | B（共享 `ReviewIssue`） | 类型契约，建议与主题 B 同批 |
| `frontend/src/views/TaskResultsView.vue` | B + E | 同上 |

---

## 数据库 Schema 迁移影响（必须处理）

1. **三处 schema 变更需落地**：`Standard.ruleLibraries`、`TaskDetail.judgeConfidence/judgeReason`、`RuleLibrary.standardId + index`。
2. 项目无 migration 历史，dev 用 `prisma db push`：
   ```
   cd backend && npx prisma db push && npx prisma generate
   ```
3. **无破坏性变更**：全是"加列/加关系"，不删列不删表。`RuleLibrary.standardId` 为可空 + `SetNull`，存量数据自动为 `null`（兼容旧审点库无标准关联）。
4. **`StandardCheckpoint` 死模型**：schema 仍保留，但已无读写。可暂不删（db push 会留表），建议后续清理以减债。

---

## 风险与待确认项清单

- [ ] **`StandardCheckpoint` 是否清理**：现为死模型，仅 `types.ts:180` 注释引用。建议删模型 + 关系 + 注释，或明确保留理由。
- [ ] **前端规则配置面板**：确认其展示的 severity 粒度与 D 主题"双 key"引擎行为一致。
- [ ] **`stats.warnings` 并发写**：E 主题两处独立 read-modify-write，当前串行低风险，并行场景需防护。
- [ ] **CRLF git 警告**：多个 backend 文件被 git 提示"LF 将被替换为 CRLF"。是 `.gitattributes`/core.autocrlf 自动转换，**不影响运行**，但提交后这些文件行尾会变 CRLF。若团队约定 LF，需检查 `.gitattributes`。
- [ ] **dist 漂移**：线上/容器跑 `dist`，改完务必 `npm run build` 并清旧 `dist`。
- [ ] **测试验证**：提交前跑 backend vitest（预期 553+ 新增）与 frontend vitest（134）确认全绿。

---

## 推荐的提交拆分方案（供参考，未执行）

若选择"按主题拆 commit"，建议顺序（依赖关系：schema 先行）：

1. `feat(schema): 审点合并关联标准 + 判标置信度字段` — `schema.prisma`（A+B 的 DB 层）
2. `refactor(checkpoint): V3.2 审点统一迁移至 rule_library_items` — A 的服务/工具/接口层
3. `feat(judge): 智能判标层与 P1-6 LOW 保留待复核` — B（含 `llm.service` 类型 + smart-judge + review 落库 + 前端）
4. `fix(consistency): P1-5 结构化一致性逐分类/逐条隔离解析` — C
5. `fix(rules): P1-8 规则前缀最长匹配与 severity 粒度` — D
6. `fix(review): P1-7 空库显式告警` — E
7. `fix(chunk): Windows CRLF 文档分片归一化` — F
8. `test: 补齐 P1 系列与机制级测试` — G

> 若选"整体一个 commit"，message 可概括为：`refactor(checkpoint): V3.2 审点合并 + 智能判标层 + P1-5/6/7/8 健壮性修复`。

---

## 结论

这批未提交改动是一次**以"V3.2 审点合并重构"为主线、附带 P1 系列（5/6/7/8）健壮性修复与智能判标新功能**的集中收尾，逻辑清晰、测试配套到位。主要风险集中在：**死模型 `StandardCheckpoint` 待清理、review.service.ts 多主题交叠导致提交拆分困难、schema 迁移与 dist 构建必须同步**。建议先跑测试 + `prisma db push`，再按上述方案提交。

*（本报告由 WorkBuddy 自动生成，未对仓库做任何写入操作。）*
