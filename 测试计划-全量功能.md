# 全量功能测试计划

> 最后更新：2026-06-02（第2轮）
> 测试目标：覆盖全部 28 个页面、~228 个 API 端点的功能验证
> 本轮新增：认证CRUD ✅ / 任务历史 ✅ / 反馈详情+附件下载 ✅ / 公告CRUD ✅ / 员工CRUD ✅ / 规则批量+测试+重置 ✅ / 误报库+术语库 ✅ / 仪表盘 ✅ / 文件清理 ✅

---

## 使用说明

- 每项测试完成后在「状态」列标注：`✅ 通过` / `❌ 失败` / `⏳ 未测` / `🚫 阻塞`
- 「备注」列记录测试数据、发现的 bug、修复 commit
- 冒烟 / 回归前在 AGENTS.md「Next Steps」注明测试范围

---

## 1. 认证与账号

| # | 功能 | 端点/页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 1.1 | 登录 | `/login` / `POST /api/auth/login` | 成功登录、错误密码、账号禁用 | ✅ | 密码错误→401，不存在账号→401，角色 ADMIN |
| 1.2 | 登出 | `POST /api/auth/logout` | 清除 token | ✅ | 二次调用 token 失效→401 |
| 1.3 | 修改密码 | `/change-password` / `POST /api/auth/change-password` | 新旧密码校验、复杂度要求 | ✅ | 旧密码错误→400，太短→400，无特殊符号→400，正确→200 |
| 1.4 | 修改用户名 | `POST /api/auth/change-username` | 唯一性校验、管理员权限 | ✅ | 缺密码→400，太短→400，修改→200，改回→200 |

---

## 2. 工作台

| # | 功能 | 页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 2.1 | 工作台首页 | `/workspace` | 待办任务数、最近任务列表、系统公告弹窗 | ✅ (API) | 任务列表10条、未读公告正常 |

---

## 3. 智能审查（核心模块）

### 3.1 创建审查任务

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 3.1.1 | 选择审查模式 | `/review` | 模式配置弹窗、`getModeCapabilities` | ⏳ | |
| 3.1.2 | 上传文件 | `/review` | 多文件上传、文件类型限制（DWG/WASM） | ⏳ | |
| 3.1.3 | 参照文件组 | `/review` | 上传参照文件 | ⏳ | |
| 3.1.4 | 标准库引用 | `/review` | 关联标准库弹窗 | ⏳ | |
| 3.1.5 | 知识库绑定 | `/review` | `SmartReviewKnowledgeDialog` | ⏳ | |
| 3.1.6 | 审查规范选择 | `/review` | `SmartReviewReviewSpecificationDialog` | ⏳ | |
| 3.1.7 | 语义规则库选择 | `/review` | `SmartReviewRuleLibraryDialog` | ⏳ | |
| 3.1.8 | 提交任务 | `POST /api/tasks` | 创建任务、文件落盘路径按 username 分组 | ✅ | 路径 /uploads/admin/... 正确 |

### 3.2 审查结果（7 种模式 API 级验证 ✅）

| # | 模式 | API/端点 | 测试文件 | 结果 | 问题数 | 备注 |
|---|---|---|---|---|---|---|
| 3.2.0a | **RULE_ONLY** 仅规则审查 | `POST /api/tasks` + `reviewMode=RULE_ONLY` | test.txt | ✅ 通过 | 0 | 纯规则执行，无 AI |
| 3.2.0b | **TYPO_GRAMMAR** 基础校对 | `POST /api/tasks` + `reviewMode=TYPO_GRAMMAR` | HX1CI084200...docx | ✅ 通过 | 21 | 错别字/一致性/完整性/格式 |
| 3.2.0c | **LIBRARY_REVIEW** 以库审文 | `POST /api/tasks` + `reviewMode=LIBRARY_REVIEW` | HX1CI084200...docx | ✅ 通过 | 17 | 完整性/错别字/一致性/交叉引用 |
| 3.2.0d | **CONSISTENCY** 一致性审查 | `POST /api/tasks` + `reviewMode=CONSISTENCY` | HX1CI084200...docx | ✅ 通过 | 23 | 一致性/交叉引用/格式/完整性 |
| 3.2.0e | **DOC_REVIEW** 以文审文 | `POST /api/tasks` + `reviewMode=DOC_REVIEW` + refFiles | SMX17CIS002...docx + 参照文件 | ✅ 通过 | 5 | 参照比对，发现不一致 |
| 3.2.0f | **MULTIMODAL** 结构化审查 | `POST /api/tasks` + `reviewMode=MULTIMODAL` | HX1CI010300...xlsx | ✅ 通过 | 6 | 交叉引用/一致性/完整性 |
| 3.2.0g | **SELF_CHECK** 标准引用自检 | `POST /api/self-check/run` | HX1CI010300...xlsx | ✅ 通过 | 6项异常/10项检查 | 匹配4/无匹配2/废止2 |

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 3.2.1 | 结果概览 Tab | `/review/:id` | 汇总统计、来源标签（规则/AI/标准） | ⏳ | |
| 3.2.2 | 问题卡片列表 | `/review/:id` | `IssueCardList` → `IssueCard`、展开/折叠、详情 | ⏳ | |
| 3.2.3 | 文件预览 | `/review/:id` | Docx/PDF/DWG/PPTX/Excel/TXT 多格式预览 | ⏳ | |
| 3.2.4 | 定位原文 | `/review/:id` | 点击问题定位到文件对应位置 | ⏳ | |
| 3.2.5 | 标准引用 Tab | `/review/:id` | 知识卡片展示 | ⏳ | |
| 3.2.6 | 误报标记 | `/review/:id` | `FalsePositiveDialog`、`toggleFalsePositive` | ⏳ | |
| 3.2.7 | 重新审查 | `PATCH /api/tasks/:id/review` | 再次触发审查 | ⏳ | |
| 3.2.8 | 导出报告 | `/review/:id` | Word/Excel 导出 | ⏳ | |
| 3.2.9 | 差异高亮 | `/review/:id` | `DiffHighlight`、`DiffText` | ⏳ | |
| 3.2.10 | 自检报告 Tab | `/review/:id` | 标准引用自检表格 | ⏳ | |
| 3.2.11 | 删除任务 | `DELETE /api/tasks/:id` | 单删/批量删（连带物理文件清理 ✅ 已修复） | ⏳ | |

### 3.3 任务历史

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 3.3.1 | 任务列表 | `/tasks` | 分页、状态过滤、搜索 | ✅ | GET /api/tasks 返回10条 |
| 3.3.2 | 任务状态查看 | `/tasks` | 进度、耗时 | ✅ | GET /api/tasks/:id/progress 正常 |

---

## 4. 知识库智能问答

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 4.1 | 对话历史 | `/langchain/qa` | 侧边栏新建/切换/删除会话 | ⏳ | |
| 4.2 | 知识库选择 | `/langchain/qa` | `KnowledgeBaseSelector` 树形选择 | ⏳ | |
| 4.3 | AI 问答 | `/langchain/qa` | 流式 SSE 输出 | ⏳ | |
| 4.4 | 引用来源展示 | `/langchain/qa` | 可折叠卡片、相似度标签、颜色分级 | ⏳ | |
| 4.5 | 搜索参数面板 | `/langchain/qa` | topK、相似度阈值、检索模式、开关（HyDE/Compression 默认关闭） | ⏳ | |
| 4.6 | 历史对话加载 | `GET /api/langchain/conversations/:id` | 恢复对话 | ⏳ | |

---

## 5. 反馈意见

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 5.1 | 提交反馈 | `POST /api/feedback` | 上传附件（MIME 白名单: pdf/docx/xlsx/png/jpg） | ✅ | 文本文件被拒（multer fileFilter），PDF/docx 正常 |
| 5.2 | 我的反馈 | `GET /api/feedback/my` | 列表、状态查看 | ✅ | |
| 5.3 | 反馈详情 | `GET /api/feedback/:id` | 下载附件 | ✅ | 附件持久化 ✅（修复 service.ts:46 逻辑反转），下载 200 |
| 5.4 | 管理员反馈管理 | `GET /api/feedback/admin/list` / `GET /api/feedback/admin/stats` | 处理/回复/批量操作（ADMIN only） | ✅ | 列表+统计正常 |

---

## 6. 系统公告

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 6.1 | 公告列表 | `/announcements` | 已读/未读标记 | ✅ | GET /admin/list 和 GET /unread 均正常 |
| 6.2 | 公告详情 | `/announcements/:id` | 阅读后未读计数更新 | ✅ | 创建→发布→详情→标记已读→删除 |
| 6.3 | 公告弹窗 | `AnnouncementPopup` | 登录后自动弹窗 | ✅ (API) | GET /api/announcements/unread 返回数组 |
| 6.4 | 全部标记已读 | `POST /api/announcements/read-all` | 批量标记 | ✅ | 修复空 body 崩溃（controller + service 增加 undefined 保护） |

---

## 7. 管理员 — 部门与员工

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 7.1 | 部门树管理 | `GET /api/departments` | 创建/编辑/删除部门（有子节点阻止删除） | ✅ | 树形结构返回正确 |
| 7.2 | 员工列表 | `GET /api/employees` | 分页、搜索、按部门筛选 | ✅ | |
| 7.3 | 创建员工 | `POST /api/employees` | 单条/批量创建 | ✅ | 创建成功，含 mustChangePassword 标记 |
| 7.4 | 编辑员工 | `PUT /api/employees/:id` | 修改信息 | ✅ | 注意是 PUT 非 PATCH |
| 7.5 | 删除员工 | `DELETE /api/employees/:id` / `POST /api/employees/batch-delete` | 单删+批量删 FK 保护 ✅ 已修复 | ✅ | admin→409阻断，user→200删除 |
| 7.6 | 批量启用/禁用 | `POST /api/employees/batch-update-status` | 批量状态更新 | ✅ | 注意是 POST 非 PATCH |
| 7.7 | 重置密码 | `POST /api/employees/reset-password/:id` | 自动生成/手动设置 | ✅ | |
| 7.8 | 批量修正用户名 | Excel 上传 | 批量改账号 | ⏳ | 需构造测试 Excel |

---

## 8. 管理员 — 标准库清单

| # | 功能 | Tab/页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 8.1 | 标准库管理 | `GET /api/standards` | CRUD、文件夹树、搜索 | ✅ | 列表返回正常 |
| 8.2 | 标准引用导入 | 批量 Excel 导入 | 模板下载、列提取、预览 | ✅ | Excel 模板下载 200/6.8KB |
| 8.3 | 规范性引用导入 | `NormativeImportDialog` | 解析标准引用 | ⏳ | |
| 8.4 | 临时库 | `GET /api/standards/temp-library` | 检验标准匹配 `checkStandard` | ✅ | |
| 8.5 | 误报库 | `GET /api/false-positive-library` | 列表/导出/删除 | ✅ | API 返回正常 |
| 8.6 | 术语库 | `GET /api/terminology` | CRUD、分类管理 | ✅ | API 返回 40 条术语 |
| 8.7 | 标准库导出 | `GET /api/standards/export/excel` | 全量导出（~2MB Excel） | ✅ | 二进制正确 |

---

## 9. 管理员 — 知识库管理

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 9.1 | 分类树 | `/admin/knowledge-categories` | 创建/编辑/删除文件夹（清理孤儿 vector_documents ✅ 已修复） | ⏳ | |
| 9.2 | 上传文档 | `/admin/knowledge-categories` | 单文件/多文件、拖拽上传 | ⏳ | |
| 9.3 | 文档预览/分段 | `ImportWizard` → `PreviewConfirmDialog` | 分段预览 | ⏳ | |
| 9.4 | 确认导入 | `PreviewConfirmDialog` | 分段后确认导入 | ⏳ | |
| 9.5 | 文档管理 | `/admin/knowledge-categories/:id/documents` | 列表/标签/搜索 | ⏳ | |
| 9.6 | 分段查看 | 文档详情 | 段落展示（XLSX 表格渲染 ✅ 已实现） | ⏳ | |
| 9.7 | 编辑段落 | 段落列表 | 修改段落内容 | ⏳ | |
| 9.8 | 删除文档 | `DELETE` | 单删/批量删（Document + VectorDocument ✅ 已修复） | ✅ | 代码确认清理逻辑 |
| 9.9 | 删除段落 | `DELETE` | 单条段落删除 | ⏳ | |
| 9.10 | 标签管理 | CRUD | 标签增删改 | ⏳ | |
| 9.11 | 生成问题 | LLM 生成 | 自动生成检索问题 | ⏳ | |
| 9.12 | Hit Test | 测试页面 | 召回命中测试 | ⏳ | |

---

## 10. 管理员 — 审查规则

| # | 功能 | 页面/端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 10.1 | 规则列表 | `GET /api/rules` | 分类查看、搜索 | ✅ | |
| 10.2 | 规则分类 | `GET /api/rules/categories` | 规则分类列表 | ✅ | |
| 10.3 | 规则面板数据 | `GET /api/rules/panel-data` | 各分类数量统计 | ✅ | |
| 10.4 | 创建规则 | `POST /api/rules` | ruleCode/name/category 必填 | ✅ | |
| 10.5 | 编辑规则 | `PATCH /api/rules/:code` | 更新名称/严重度/启用状态 | ✅ | 注意是 PATCH 非 PUT |
| 10.6 | 删除规则 | `DELETE /api/rules/:code` | 同时标记关联 taskDetail | ✅ | |
| 10.7 | 启用/禁用 | `POST /api/rules/batch-toggle` / `POST /api/rules/:code/toggle` | 单条/批量/按前缀 | ✅ | POST /batch-toggle 正确 |
| 10.8 | 规则导入 | `POST /api/rules/import` | 批量导入 | ✅ (端点存在) | GET /import/template 返回模板 |
| 10.9 | 规则测试 | `POST /api/rules/test-match` | 实时测试正则匹配 | ✅ | POST /test-match 返回结果 |
| 10.10 | 恢复默认 | `POST /api/rules/reset` | 重置所有规则 | ✅ | |

---

## 11. 管理员 — AI 引擎配置

| # | 功能 | Tab/页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 11.1 | 对话模型 | `GET /api/system-config/llm_chat_model` | API 地址/Key/模型名配置 | ✅ | 配置可读 |
| 11.2 | Embedding 模型 | `GET /api/system-config/embedding_model` | 配置（默认 BAAI/bge-m3） | ✅ | 配置可读 |
| 11.3 | Reranker 模型 | `GET /api/system-config/reranker_model` | 配置（默认 BAAI/bge-reranker-v2-m3） | ✅ | 配置可读 |
| 11.4 | 视觉模型 | `GET /api/system-config/llm_ocr_model` | 配置 | ⏳ | |
| 11.5 | OCR 配置 | `OcrConfigTab` | PaddleOCR 服务地址 | ✅ | `GET /api/system-config/ocr-status` 返回正常 |
| 11.6 | 系统基础设置 | `GET /api/system-config/basic_settings` | 通用配置 | ✅ | |
| 11.7 | LLM 测试 | `POST /api/system-config/test-llm` / `test-llm-send` | 发送测试消息验证连接 | ⏳ | 需 LLM key |
| 11.8 | 提示词模板 | `GET /api/prompt-templates` / `GET /api/prompt-templates/modules` | 查看/编辑/重置/启用禁用 | ✅ | 列表返回正常 |

---

## 12. 管理员 — 其他设置

| # | 功能 | 页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 12.1 | 数据看板 | `/admin/dashboard` | 统计/趋势图表 | ✅ | GET /api/dashboard/stats 和 /trend 均正常 |
| 12.2 | 系统设置 | `/admin/system` | 通用配置 | ✅ | `GET /api/system-config/basic_settings` |
| 12.3 | 存储管理 | `/admin/storage` | 路径配置/修改/子目录初始化 ✅ 已实现 | ✅ | Windows/Linux 路径均支持 |
| 12.4 | 基础设置 | `/admin/basic` | 文件白名单等 | ✅ | `GET /api/system-config/basic_settings` |
| 12.5 | 审计日志 | `GET /api/audit-logs` | 列表/筛选/导出 | ✅ | |
| 12.6 | 语义规则库 | `GET /api/rule-libraries` | CRUD | ✅ | 列表返回正常 |
| 12.7 | 审查规范 | `GET /api/review-specifications` | CRUD | ✅ | 列表返回正常 |

---

## 13. 自检功能

| # | 功能 | 端点/页面 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 13.1 | 运行自检 | `POST /api/self-check/run` | 上传文件→标准引用比对（绝对路径 bug ✅ 已修复） | ✅ | 10项检查: 4匹配/6异常(2废止+4无匹配) |
| 13.2 | 自检报告 | `/self-check` | 结果统计、详情、导出 | ⏳ | |

---

## 14. LLM Proxy 代理

| # | 功能 | 端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 14.1 | Chat 代理 | `POST /api/llm-proxy/chat/completions` | LLM 透传 | ⏳ | 需 LLM key |
| 14.2 | Embedding 代理 | `POST /api/llm-proxy/embeddings` | 向量化透传 | ⏳ | 需 LLM key |
| 14.3 | Rerank 代理 | `POST /api/llm-proxy/rerank` | 重排透传 | ⏳ | 需 LLM key |
| 14.4 | 代理状态 | `GET /api/llm-proxy/status` | 连接状态 | ✅ | Chat/Embedding 已配置 |

---

## 15. 系统健康与监控

| # | 功能 | 端点 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|---|
| 15.1 | 健康检查 | `/health`, `/api/health` | 服务状态 | ✅ | |
| 15.2 | 指标监控 | `/api/metrics` | 运行时指标 | ✅ | |
| 15.3 | 缓存管理 | `/api/cache/stats`, `/api/cache/clear` | 缓存状态/清理 | ✅ | |
| 15.4 | 存储统计 | `/api/system/storage-stats` | 存储用量 | ✅ | |
| 15.5 | 文件清理 | `POST /api/system/cleanup-files` | 清理孤立文件 | ✅ | 返回 200，已清理 0 个孤立文件 |

---

## 16. 存储管理（本次改造核心）

| # | 功能 | 测试要点 | 状态 | 备注 |
|---|---|---|---|---|
| 16.1 | 默认路径初始化 | 启动时从 DB 读取 `upload_path`，创建 8 个子目录 | ✅ 通过 | |
| 16.2 | 管理员修改路径 | `GET/PUT /api/system/config-path` → 写入 DB → 热生效 | ✅ 通过 | Windows/Linux 路径格式均支持 |
| 16.3 | 切换路径自动创建子目录 | 8 个子目录 + 写入校验 | ✅ 通过 | |
| 16.4 | 按用户名分组 | `uploads/admin/{filename}` 非 UUID 目录 | ✅ 通过 | 7 种模式测试均验证 |
| 16.5 | 静态服务热切换 | 修改路径后 `/uploads/*` 指向新目录 | ✅ 通过 | 路径切换后文件正常写入 |
| 16.6 | 路径必须为绝对路径校验 | Windows `E:\...` 和 Linux `/home/...` 均支持 | ✅ 已修复 | `system.routes.ts:148` |
| 16.7 | 旧路径文件统计 | 切换时显示旧路径文件数 | ✅ 通过 | PUT 返回 `filesAtOldPath: 19` |
| 16.8 | 文件迁移脚本 | `node scripts/migrate-uploads.js` | ⏳ 未测 | 需 server 重启后执行 |

---

## 17. CRUD 写入操作验证

| # | 模块 | 操作 | 状态 | 备注 |
|---|---|---|---|---|
| 17.1 | 部门 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/departments` |
| 17.2 | 标准 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/standards` |
| 17.3 | 知识分类 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/knowledge-categories` |
| 17.4 | 审查规则 | 创建/更新(PATCH)/删除 | ✅ | `POST/PATCH/DELETE /api/rules/:code` |
| 17.5 | 审查规范 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/review-specifications` |
| 17.6 | 语义规则库 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/rule-libraries` |
| 17.7 | 反馈 | 创建(无文件/PDF附件)/删除 | ✅ | MIME白名单校验正常；`text/plain` 被拒 |
| 17.8 | 系统配置 | 更新 | ✅ | `PUT /api/system-config/:key` |
| 17.9 | 公告 | 创建/更新/删除 | ✅ | `POST/PUT/DELETE /api/announcements` |

---

## 附录

### 数据完整性修复清单

| # | 问题 | 修复位置 | 验证方法 | 状态 | 备注 |
|---|---|---|---|---|---|
| HIGH #1 | 删除任务不清理 uploads 物理文件 | `task.service.ts:648-673` | 删任务后检查文件系统 | ✅ 通过 | 创建→删除→确认文件已删除 |
| HIGH #2 | 删除任务不清理误报库记录 | `task.service.ts:648-673` | 查 FalsePositiveLibrary 表 | ✅ 通过 | 代码确认清理逻辑 |
| HIGH #3 | 批量删除员工 FK 冲突 | `employee.service.ts:304-340` | 批量删含 QA 会话的员工 | ✅ 通过 | admin(有任务)→409拒绝; user(无依赖)→200删除 |
| HIGH #4 | 删除知识分类残留孤儿 vector_documents | `knowledge-category.service.ts:258-263` | 删分类前清 documentId=NULL 的向量 | ✅ 通过 | 代码确认 `documentId: null` 清理逻辑 |
| MEDIUM #5 | Standard 删除不置空关联 | `standard.service.ts:280` | 删除前 `updateMany set standardId=null` | ✅ 通过 | 代码确认 |
| MEDIUM #6 | ReviewSpecification 删除不置空关联 | `review-specification.service.ts:296` | 删除前 `updateMany set reviewSpecificationId=null` | ✅ 通过 | 代码确认 |
| MEDIUM #7 | ReviewRule 删除不置空关联 | `rule.controller.ts:397-400` | 删除前 taskDetail 标记已删除 | ✅ 通过 | 代码确认 `(已删除)前缀` |
| MEDIUM #8 | FileCleanupService 未跟踪 Feedback 附件 | `file-cleanup.service.ts:30-52` | 清理时遍历 `attachmentPaths` | ✅ 通过 | 代码确认 |
| BUG #9 | 反馈附件未持久化到 DB | `feedback.service.ts:46` | attachmentPaths 逻辑反转 | ✅ 已修复 | `JsonNullValueInput.JsonNull` 与 `input.attachmentPaths` 颠倒 |
| BUG #10 | 公告 read-all 空 body 崩溃 | `announcement.controller.ts:242` + `.service.ts:302` | req.body 为 undefined 时解构崩溃 | ✅ 已修复 | 增加 req.body?.announcementIds 可选 + service 接受 undefined 标记全部已读 |

### 本次改造功能清单

| # | 改动 | 状态 | 备注 |
|---|---|---|---|
| 1 | `backend/src/config/upload.ts` — 可变 uploadDir、热切换 | ✅ 已实现 | |
| 2 | 8 个 multer destination 改为 config 函数 | ✅ 已实现 | |
| 3 | 5 个 controller 硬编码 UPLOAD_DIR → `getUploadPath()` | ✅ 已实现 | |
| 4 | ~15 个 service 位置 → `resolveFilePath()` / `getUploadPath()` | ✅ 已实现 | |
| 5 | `file-cleanup.service.ts` 重写为递归扫描子目录 | ✅ 已实现 | |
| 6 | `POST /api/system/config-path` / `GET /system/config-path` | ✅ 已实现 | |
| 7 | `StorageManagement.vue` 存储路径管理页面 | ✅ 已实现 | |
| 8 | JWT payload 新增 username 字段 | ✅ 已实现 | |
| 9 | `index.ts` 启动时从 DB 读 upload_path 初始化子目录 | ✅ 已实现 | |
| 10 | XLSX 质量检查跳过（score:100） | ✅ 已实现 | |
| 11 | XLSX 内容为 Markdown 表格入库 + 前端表格渲染 | ✅ 已实现 | |
| 12 | 知识库删除文档同步清理 documents 表 | ✅ 已实现 | |
| 13 | QA 默认关闭 HyDE/Compression，MultiQuery=1 | ✅ 已实现 | |
| 14 | QA 引用来源可折叠卡片 + 分色 | ✅ 已实现 | |
| 15 | Self-check 绝对路径 bug 修复 | ✅ 已实现 | |
