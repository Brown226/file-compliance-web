# 审查模式全量分析报告

> 分析日期：2026-05-23 | 项目版本：v2.0 | 分析范围：全项目代码

---

## 一、当前审查模式的实际调用路径与使用场景清单

### 1.1 体系概览：三层数据流

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          前端 (SmartReview.vue)                          │
│                                                                         │
│  旧流程: reviewItems 勾选 → derivedMode() 推导 legacyMode 字符串           │
│  新流程: reviewPlanDraft 对象 → submitPlan.objective 映射 legacyMode       │
│                                                                         │
│  ┌───────────────────────┐     ┌──────────────────────────────────┐    │
│  │ 旧流程 (useNewFlow=false) │     │ 新流程 (useNewFlow=true)           │    │
│  │  derivedMode → 6种字符串  │     │  submitPlan → objective → 6种字符串 │    │
│  └───────────┬───────────┘     └────────────────┬─────────────────┘    │
│              └────────────────┬─────────────────┘                        │
│                               ▼ (FormData reviewMode + reviewPlan JSON)  │
├─────────────────────────────────────────────────────────────────────────┤
│                          后端 (task.controller.ts)                       │
│                               │                                         │
│                               ▼                                         │
│                     TaskService.createTask()                             │
│                     ├─ normalizeReviewPlan() → ReviewPlan 对象           │
│                     ├─ planToLegacyMode() → reviewMode 字符串            │
│                     └─ 存入 task.reviewMode + task.reviewPlan            │
├─────────────────────────────────────────────────────────────────────────┤
│                      后端 (review.service.ts)                            │
│                               │                                         │
│                               ▼                                         │
│                     adaptReviewPlanForExecution()                        │
│                     ├─ 从 reviewPlan 反推 reviewMode 字符串               │
│                     ├─ 推导 executionOverrides (RULE_ONLY → 关AI)        │
│                     └─ 传入 PipelineContext                             │
│                               │                                         │
│                               ▼                                         │
│                     Pipeline 工厂 (factory.ts)                           │
│                     ├─ PIPELINE_MAP[mode] → ReviewPipeline 实例          │
│                     └─ LIBRARY/CONSISTENCY/CUSTOM → StandardPipeline()  │
├─────────────────────────────────────────────────────────────────────────┤
│                    Pipeline 执行 (base-pipeline.ts)                       │
│                    capabilities 驱动两阶段编排                             │
│                    阶段1: 文本提取→规则/stdRef(并行)→after钩子            │
│                    阶段2: aiStrategy分发→AI审查→postProcess              │
│                    + review.service.ts 额外跨文件一致性                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 六种审查模式总览

| 模式 | 数据库枚举值 | Pipeline 类 | capabilities（静态） | 实际执行的审查逻辑 |
|------|-------------|-------------|-------------------|------------------|
| **LIBRARY_REVIEW** | `LIBRARY_REVIEW` | `StandardPipeline` | rules:off, stdRef:off, ai:standard | 仅阶段2 AI(RAG→LLM)审查 |
| **DOC_REVIEW** | `DOC_REVIEW` | `DocReviewPipeline` | rules:off, stdRef:off, ai:refCompare | 仅阶段2 参照比对AI（无参照降级标准AI） |
| **CONSISTENCY** | `CONSISTENCY` | `StandardPipeline` | rules:off, stdRef:off, ai:standard, crossFile:on | 仅阶段2 AI审查 + 跨文件一致性检查 |
| **TYPO_GRAMMAR** | `TYPO_GRAMMAR` | `TypoGrammarPipeline` | rules:off, stdRef:off, ai:llmOnly | 仅阶段2 纯LLM审查 + 术语白名单过滤 |
| **MULTIMODAL** | `MULTIMODAL` | `MultimodalPipeline` | rules:off, stdRef:off, ai:multimodal | 仅阶段2 多模态LLM + 表格/公式检测钩子 |
| **CUSTOM_RULE** | `CUSTOM_RULE` | `StandardPipeline` | rules:on, stdRef:on, ai:false | **仅阶段1** 规则引擎 + 标准引用检查 |

### 1.3 关键调用路径清单

#### 路径 A：存储层（提交→入库）

```
SmartReview.vue:1369-1379  (legacyMode 字符串 → FormData)
    → task.controller.ts:13  (req.body.reviewMode)
    → TaskService.createTask() task.service.ts:200
        → normalizeReviewPlan():144 (reviewPlan 对象)
        → 存 task.reviewMode + task.reviewPlan:285
```

#### 路径 B：执行层（审查触发→执行）

```
ReviewService.processTask() review.service.ts:257
    → adaptReviewPlanForExecution():26 (reviewPlan→reviewMode字符串反推:44)
    → createPipelineAsync(reviewMode):584 (工厂创建Pipeline)
    → 阶段1: runFileFastPhase():757 → pipeline.runFastPhase():832
        → BasePipeline.runFastPhase() (能力驱动编排):296
    → 阶段2: runFileSlowPhase() → pipeline.runSlowPhase()
        → BasePipeline.runSlowPhase():356 → runAIStrategy()→ capabilities分发:103
    → 跨文件: CrossFileConsistencyService.check():662
```

#### 路径 C：配置层（能力覆盖）

```
mode-config.ts: MODE_CAPABILITIES (静态常量)
    ↓ 合并
mode-config.service.ts: getModeCapabilitiesConfig() (DB systemConfig 覆盖)
    ↓ 30秒缓存
factory.ts: getRuntimeCapabilities() → PIPELINE_MAP[mode](caps)
    ↓
BasePipeline: capabilities 驱动 runFastPhase/runSlowPhase
    ↓ 额外检查
pipeline-config.ts: shouldRunStage() (pipeline_review_config + executionOverrides)
```

### 1.4 模式推导路径汇总

| 前端条件 | derivedMode / legacyMode | ReviewPlan.objective | Pipeline | 实际行为 |
|---------|--------------------------|---------------------|----------|---------|
| 仅勾选"标准审查" | LIBRARY_REVIEW | COMPLIANCE | StandardPipeline | 仅 AI(RAG) |
| 仅勾选"参照比对" | DOC_REVIEW | COMPARE | DocReviewPipeline | AI 参照比对 |
| 仅勾选"错别字检查" | TYPO_GRAMMAR | PROOFREAD | TypoGrammarPipeline | 纯 LLM |
| 仅勾选"图纸识别" | MULTIMODAL | STRUCTURED | MultimodalPipeline | 多模态 LLM |
| 仅勾选"规则库" | CUSTOM_RULE | COMPLIANCE(profile=RULE_ONLY) | StandardPipeline | 仅规则引擎 |
| 多项同时勾选 | **CONSISTENCY** | COMPLIANCE(intra+跨文件) | StandardPipeline | AI + 跨文件一致性 |
| 新流程 objective=COMPLIANCE | LIBRARY_REVIEW | COMPLIANCE | StandardPipeline | 仅 AI |

---

## 二、不合理使用的具体代码位置与原因分析

### 🔴 问题1：双重模式系统造成的不必要往返转换

**位置：**
- `task.service.ts:12-26` `planToLegacyMode()` — ReviewPlan → 字符串
- `task.service.ts:28-141` `legacyModeToPlan()` — 字符串 → ReviewPlan
- `review.service.ts:26-68` `adaptReviewPlanForExecution()` — 又从 ReviewPlan 反推回字符串

**问题描述：**
前端同时发送 `reviewMode`（字符串）和 `reviewPlan`（对象）。后端入库时用 `normalizeReviewPlan()` 统一为 ReviewPlan 对象，但执行时 `adaptReviewPlanForExecution()` 又调用 `planToLegacyMode()` 把对象反推回字符串，传给 `createPipelineAsync(reviewMode)`。整个链路存在 **"字符串→对象→字符串"** 的往返损耗。

**影响：** 增加维护成本。新增审查目标（objective）时需要在 3 个地方同步更新映射逻辑：
1. `planToLegacyMode()` — 正向映射
2. `legacyModeToPlan()` — 反向映射
3. 前端 `legacyMode` 推导逻辑

**建议：** Pipeline 工厂应直接接受 `ReviewPlan` 对象而非字符串，工厂内部根据 `objective + capabilities` 决定创建哪个 Pipeline 实例。可逐步淘汰 `reviewMode` 字符串，将其降级为仅显示友好的中文字符串。

---

### 🔴 问题2：StandardPipeline 包含永远不执行的死代码

**位置：** `standard.pipeline.ts:32-36`

```typescript
const modeInfo: Record<ReviewModeType, { name: string; desc: string }> = {
  LIBRARY_REVIEW: { ... },
  CONSISTENCY: { ... },
  CUSTOM_RULE: { ... },
  // 以下两个不会实际用到（DocReview/Multimodal/TypoGrammar 有各自 Pipeline），
  // 保留声明以防 factory 误传
  DOC_REVIEW: { name: '以文审文', ... },
  TYPO_GRAMMAR: { name: '错别字/语法', ... },
  MULTIMODAL: { name: '多模态识别', ... },
};
```

**问题描述：** 代码注释明确说 DOC_REVIEW/TYPO_GRAMMAR/MULTIMODAL "不会实际用到"，但保留了 3 个冗余条目。这增加了认知负担：开发者读代码时需要判断哪些是实际执行的路径。

**建议：** 将 `modeInfo` 改为只包含实际可能传入 StandardPipeline 的模式（LIBRARY_REVIEW/CONSISTENCY/CUSTOM_RULE），或使用 `Partial<Record<...>>` + 回退默认值。如果"防止 factory 误传"是真实需求，应在 factory.ts 用断言保护而非在构造器里兜底。

---

### 🔴 问题3：BasePipeline.runAIStrategy() 有两个无效分支

**位置：** `base-pipeline.ts:103-122`

```typescript
protected async runAIStrategy(text, ctx) {
  switch (this.capabilities.aiStrategy) {
    case 'llmOnly':     return this.runLLMOnlyStrategy(text, ctx);
    case 'refCompare':  return this.runAIReview(text, ctx);  // ← 无效分支
    case 'multimodal':  return this.runAIReview(text, ctx);  // ← 无效分支
    case 'standard':
    default:            return this.runAIReview(text, ctx);
  }
}
```

**问题描述：** `refCompare` 和 `multimodal` 两个分支在 BasePipeline 中都降级到 `runAIReview()`。但实际上：
- `refCompare` 只会出现在 `DocReviewPipeline`（覆盖了 `runAIStrategy()`），不会走 BasePipeline
- `multimodal` 只会出现在 `MultimodalPipeline`（覆盖了 `runAIStrategy()`），不会走 BasePipeline
- 这两个分支是永远无法被执行到的**无效降级代码**

**建议：** 删除 `refCompare` 和 `multimodal` 分支，或改为抛出明确异常（"此策略需要子类覆盖 runAIStrategy()"）。这能消除"这个降级到底是什么行为"的困惑。

---

### 🟡 问题4：Pipeline 在单次审查中被重复创建 2-3 次

**位置：**
- `review.service.ts:584` — `createPipelineAsync(reviewMode)` 用于检查 `pipeline.capabilities.ai`
- `review.service.ts:786` — `runFileFastPhase()` 中再次 `createPipelineAsync(ctx.reviewMode)`
- `review.service.ts:970+` — `runFileSlowPhase()` 中又创建一次

**问题描述：**
一次任务审查中，同一个 reviewMode 的 Pipeline 被创建了至少 3 次（全局1次 + 每文件阶段1×1 + 每文件阶段2×1）。虽然有 30 秒缓存，但这是不优雅的重复创建。且 `review.service.ts:584` 创建 Pipeline 仅为了读 `capabilities.ai`（这一行代码就做了 DB 查询+工厂构建），完全可以通过读取静态 `MODE_CAPABILITIES` 来替代。

**影响：** 不必要的 `getRuntimeCapabilities()` DB 查询、不必要的对象构造。

**建议：** 在 `processTask()` 入口处创建一次 Pipeline 并缓存，后续复用同一个实例。第 584 行改用 `getModeCapabilities(reviewMode).ai` 直接读取静态配置。

---

### 🟡 问题5：CONSISTENCY 模式语义混淆

**位置：** `SmartReview.vue:961-978` derivedMode

```typescript
const derivedMode = computed(() => {
  // ...
  if (active.length === 1) {
    if (active[0] === 'standardReview') return 'LIBRARY_REVIEW'
    // ...
  }
  // 多项 → CONSISTENCY（启用跨文件）
  return 'CONSISTENCY'
})
```

**问题描述：** **"多项审查同时启用"** 被强制映射为 `CONSISTENCY` 模式。但 `CONSISTENCY` 模式的核心能力是 `crossFile: true`（跨文件一致性检查），它并不等于"组合审查"。用户可能只想同时做"错别字检查 + 标准审查"，应该走 LIBRARY_REVIEW 而不是 CONSISTENCY。

**影响：** 
1. 语义不正确："多选审查项"≠"需要跨文件一致性"
2. 审查模板可能错配：CONSISTENCY 模式 scene 为 `consistency`，加载的提示词模板与 LIBRARY_REVIEW 不同
3. 用户勾选"标准审查+错别字检查"时，被强制执行一个未预期的跨文件一致性扫描

**建议：** `derivedMode` 多项勾选时应回退到 `LIBRARY_REVIEW`（组合能力最全的基础模式），CONSISTENCY 应有独立的勾选项或仅在 `crossFileConsistency` 启用时触发。

---

### 🟡 问题6：`standardRef: 'config'` 三态枚举值从未被实际使用

**位置：**
- `mode-config.ts:21` — `standardRef: 'on' | 'off' | 'config'`
- `mode-config.ts:47-96` — 六种模式的 `standardRef` 全部为 `'on'` 或 `'off'`，无一是 `'config'`
- `mode-config.service.ts:95` — `saveModeCapabilitiesConfig()` 中仅保留 'config'/'off'/'on' 三态
- `base-pipeline.ts:328` — `if (this.capabilities.standardRef === 'off') return [];` 分支处理
- `base-pipeline.ts:385` — `if (this.capabilities.standardRef === 'config' && ...)` 分支处理

**问题描述：** `config` 枚举值定义了完整的 DB 读写 + 运行时分支逻辑，但六种模式的**静态默认配置和 DB 覆盖配置中均从未设置为 `config`**。这 ~30 行分支代码是未经测试、从未执行的死代码。

**建议：** 要么删除 `config` 状态并简化三态为布尔（`standardRef: boolean`），要么至少在一个模式上实际启用 `config` 并编写测试覆盖。当前状态是最坏情况：增加了代码复杂度但零价值。

---

### 🟡 问题7：rulePrefixes 属性已完成弃用但未清理

**位置：**
- `types.ts:161` — `ReviewPipeline.rulePrefixes: string[]`（标记 `@deprecated`）
- `base-pipeline.ts:52` — `readonly rulePrefixes: string[] = [];`（标记 `@deprecated`）
- `types.ts:28` — `PipelineModeConfig.rulePrefixes: string[]`
- `pipeline-config.ts:72-83` — `getEffectiveRulePrefixes()`

**问题描述：** `rulePrefixes` 在接口、基类、配置工具中都已标记为 `@deprecated`，说明 "实际过滤由数据库 review_rules.enabled 控制"。但它仍然定义了完整的读取链路（`getEffectiveConfig()` → `PipelineModeConfig.rulePrefixes` → `runFastPhase()` 实际调用）。6 个文件需要为这个已弃用字段维护代码。

**建议：** 确认 `rulePrefixes` 已无消费方后，在所有相关接口/类中彻底移除该字段，删除 `getEffectiveRulePrefixes()` 工具函数。

---

### 🟡 问题8：三层配置覆盖机制中"执行期动态覆盖"被绕过

**位置：**
- `review.service.ts:47-49` — `adaptReviewPlanForExecution()` 构建 `executionOverrides.stages`
- `review.service.ts:388` — 设置 `executionOverrides` 到 `PipelineContext`
- `pipeline-config.ts:50-52` — `shouldRunStage()` 的确读取了 `executionOverrides`

**BUT：** `review.service.ts:588` 在阶段2入口处，使用 `pipeline.capabilities.ai`（静态）代替 `shouldRunStage()` 检查：

```typescript
// review.service.ts:584-588
const pipeline = await createPipelineAsync(reviewMode as any);
const needsAI = pipeline.capabilities.ai;   // ← 直接读 capabilities，绕过了 shouldRunStage!
if (!needsAI) {
  // 跳过整个阶段2...
}
```

而 `shouldRunStage()` 实际上会先检查 `executionOverrides.stages.ai`。这意味着：即使在 `pipeline_review_config` 中禁用了某模式的 AI，只要 capabilities 声明 `ai: true`，阶段2仍会执行（只不过在 `runSlowPhase()` 内部被二次检查拦截）。

**影响：** 跳过逻辑不一致。`review.service.ts` 的入口判断与 `base-pipeline.ts` 的内部分支使用不同的判断源，可能导致行为不一致（例如进度推送消息错位）。

**建议：** 统一使用 `shouldRunStage(ctx, 'ai')` 替代 `pipeline.capabilities.ai`，确保 `executionOverrides` 在入口处也生效。

---

### 🟢 问题9：DOC_REVIEW 模式的状态管理依赖隐式约定

**位置：** `task.service.ts:244`

```typescript
const shouldDelayReview = resolvedReviewMode === 'DOC_REVIEW';
```

**问题描述：** DOC_REVIEW 模式需要先上传参照文件才能审查，因此创建任务时不触发 `addReviewJob()`。但判定条件硬编码为 `resolvedReviewMode === 'DOC_REVIEW'`。如果未来有新模式也需要延迟审查（如 MULTIMODAL 需要大模型预加载），这种硬编码就失效了。

**建议：** 将 "需要延迟审查" 标记为 ModeCapabilities 的一个属性（如 `needsDelayedExecution: boolean`），由配置驱动而非硬编码模式字符串。

---

### 🟢 问题10：reviewPlan → reviewMode 反向推导的可逆性不保证

**位置：** `task.service.ts:12-26` planToLegacyMode()

```typescript
static planToLegacyMode(plan: ReviewPlan): string {
  if (plan.objective === 'COMPARE') return 'DOC_REVIEW';
  if (plan.objective === 'PROOFREAD') return 'TYPO_GRAMMAR';
  if (plan.objective === 'STRUCTURED') return 'MULTIMODAL';
  if (plan.execution.profile === 'RULE_ONLY' && plan.evidence.sources.includes('REVIEW_SPECIFICATION')) {
    return 'CUSTOM_RULE';
  }
  if (plan.enhancements.crossFileConsistency) return 'CONSISTENCY';
  // ...
  return 'LIBRARY_REVIEW';
}
```

**问题：** 这个函数是 **单向的、有信息损失的** 推导。例如：
- `LIBRARY_REVIEW` 和 `CONSISTENCY` 的 plan 都可能是 `objective=COMPLIANCE`
- 如果 `crossFileConsistency=true`，返回 CONSISTENCY；否则返回 LIBRARY_REVIEW

但如果 `plan.enhancements.crossFileConsistency=true` 而 pipeline 用的是 StandardPipeline，实际执行时跨文件检查在 `review.service.ts:664` 通过 `executionPlan.crossFileConsistency || pipeline.capabilities.crossFile` 双重判定，这里 `executionPlan` 才是真正控制跨文件检查的。`planToLegacyMode` 的返回结果仅用于选择 Pipeline 实例，不直接控制跨文件检查行为。

**影响：** 低。目前逻辑因为 `StandardPipeline(CONSISTENCY).capabilities.crossFile=true` 和 `executionPlan.crossFileConsistency=true` 同时为真而保持了一致，但这种双重判定（review.service.ts:664 的 `||` 逻辑）本身就是冗余的。

---

## 三、值得优化修改的具体建议与预期效果

### 建议1：【高优先级】统一 Pipeline 创建入口，消除重复构建

**目标文件：** `review.service.ts`

**修改方案：**
```typescript
// processTask() 入口处：
const pipeline = await createPipelineAsync(reviewMode as any);
// 后续 runFileFastPhase / runFileSlowPhase 接受 pipeline 参数，不再各自创建

// 584行改为：
const needsAI = getModeCapabilities(reviewMode as any).ai;  // 直接读静态配置
```

**预期效果：**
- 每个任务的 Pipeline 创建从 O(N×3) 降为 O(1)
- 减少 30 秒缓存窗口期内不必要的 DB 查询
- 代码更清晰地表达"一次选择，全程复用"

---

### 建议2：【高优先级】修复 CONSISTENCY 模式语义混淆

**目标文件：** `SmartReview.vue:957-978` derivedMode

**修改方案：**
```typescript
// 多项勾选时回退到 LIBRARY_REVIEW，而非 CONSISTENCY
if (active.length >= 2) {
  // 如果勾选了跨文件一致性，则用 CONSISTENCY
  if (reviewItems.intraFileConsistency.enabled) return 'CONSISTENCY'
  return 'LIBRARY_REVIEW'  // 基础组合模式
}
```

**或者更彻底地：将 crossFileConsistency 作为独立开关，不影响模式选择。** CONSISTENCY 应该像 `intraFileConsistency` 一样是一个独立的 enhancement 开关，而不是一种审查模式。

**预期效果：**
- 用户选择"标准审查+错别字检查"时，不再被意外强制执行跨文件一致性
- 审查提示词模板与用户预期一致（不再加载 consistency 场景模板）
- 减少用户困惑

---

### 建议3：【中优先级】删除无效代码路径

**涉及文件：**
| 文件 | 死代码位置 | 行数 |
|------|-----------|------|
| `standard.pipeline.ts` | modeInfo 中 DOC_REVIEW/TYPO_GRAMMAR/MULTIMODAL 条目 | ~9行 |
| `base-pipeline.ts` | runAIStrategy() 中 refCompare/multimodal 降级分支 | ~8行 |
| `mode-config.ts` | standardRef 'config' 枚举值（及其在 base-pipeline 中的对应分支） | ~20行 |
| `base-pipeline.ts` + `types.ts` + `pipeline-config.ts` | rulePrefixes 弃用属性 | ~30行 |

**修改方案：**
1. standard.pipeline.ts: 将 `modeInfo` 改为 `Partial<Record<...>>`，未匹配时抛出错误
2. base-pipeline.ts: 删除 refCompare/multimodal 的无效 default 分支
3. mode-config.ts: 将 `standardRef` 从三态枚举简化为 `boolean`
4. 全局清理 rulePrefixes

**预期效果：**
- 消除 ~70 行死代码/弃用代码
- 降低新开发者理解成本
- 减少 CI/lint 警告

---

### 建议4：【中优先级】消除前端 reviewMode 字符串与 ReviewPlan 对象的双重传递

**目标文件：** `SmartReview.vue:1379` + `task.controller.ts` + `task.service.ts`

**修改方案：**
1. 前端不再发送 `reviewMode` 字段（FormData），仅发送 `reviewPlan`
2. 后端 `createTask()` 内部通过 `normalizeReviewPlan()` 统一推导模式
3. 将 `task.reviewMode` 字段标记为数据库计算列或改为仅存储显示名

**预期效果：**
- 前端减少约 15 行模式推导逻辑
- 后端 `legacyModeToPlan()` / `planToLegacyMode()` 互转函数可合并为一个单向函数
- 消除 3 处映射逻辑不一致的风险

---

### 建议5：【低优先级】将跨文件一致性从"模式能力"改为"增强开关"

**目标文件：** `mode-config.ts` + `review.service.ts`

**现状：** `crossFile` 是 ModeCapabilities 的属性，仅 CONSISTENCY 模式启用。但在执行时，`review.service.ts:664` 使用 `executionPlan.crossFileConsistency || pipeline.capabilities.crossFile` 双重判定，说明执行层已经支持动态控制。

**修改方案：**
1. 从 MODE_CAPABILITIES 中移除 `crossFile` 属性
2. 跨文件一致性完全由 `ReviewPlan.enhancements.crossFileConsistency` 控制
3. CONSISTENCY 降级为 LIBRARY_REVIEW + crossFileConsistency=true

**预期效果：**
- 减少一种审查模式（CONSISTENCY 不再是独立模式）
- 跨文件一致性变为所有模式都能开启的通用能力
- 前端选择更直观：勾选"跨文件一致性检查"即可

---

### 建议6：【低优先级】DocReviewPipeline 无参照时的降级行为需明确定义

**目标文件：** `doc-review.pipeline.ts`

**现状：** 无参照文件时，降级为 `runAIReview()`（标准 AI 审查）。但这个行为没有被明确通知给用户——用户可能以为自己在做"参照比对"，实际执行的是"标准审查"。

**修改方案：**
1. 在 PipelineContext/Result 中添加 `degraded: boolean` 标识
2. 降级时写入一条 TaskDetail（severity=info），告知用户"因缺少参照文件，已降级为标准 AI 审查"
3. 前端展示降级提示

---

## 四、汇总

| 编号 | 问题 | 严重度 | 影响范围 | 修改工作量 |
|------|------|--------|---------|-----------|
| 1 | ReviewPlan↔reviewMode 双重系统往返转换 | 🔴 高 | task.service.ts, review.service.ts, SmartReview.vue | 中 |
| 2 | StandardPipeline 死代码（永不执行的 modeInfo 条目） | 🔴 中 | standard.pipeline.ts | 小 |
| 3 | BasePipeline 无效降级分支（refCompare/multimodal） | 🔴 中 | base-pipeline.ts | 小 |
| 4 | Pipeline 重复创建 2-3 次/文件 | 🟡 中 | review.service.ts | 小 |
| 5 | CONSISTENCY 模式语义混淆（多项→跨文件） | 🟡 高（用户可见） | SmartReview.vue | 小 |
| 6 | standardRef 'config' 枚举值从未使用 | 🟡 低 | mode-config.ts, base-pipeline.ts | 小 |
| 7 | rulePrefixes 已完成弃用但未清理 | 🟡 低 | 6个文件 | 中 |
| 8 | 阶段2 AI判断绕过 executionOverrides | 🟡 中 | review.service.ts:584 | 小 |
| 9 | DOC_REVIEW 延迟审查硬编码 | 🟢 低 | task.service.ts:244 | 小 |
| 10 | planToLegacyMode 单向不可逆 | 🟢 低 | task.service.ts:12-26 | 小 |

**建议实施顺序：** 建议2（用户可见bug）→ 建议1（性能优化）→ 建议3（清理死代码）→ 建议4+5（架构简化）→ 建议6+8（完善降级逻辑）

---

## 五、总体设计评价

**优点：**
1. 能力驱动架构（ModeCapabilities）是一次成功的重构，将 4 个重复 Pipeline 合并为 StandardPipeline，大幅降低代码重复
2. 钩子方法模式（beforeFastPhase/runAIStrategy/postProcessAIResult）设计良好，子类差异化清晰
3. Pipeline 工厂 + 缓存机制合理，支持运行时 DB 覆盖
4. 两阶段并行编排（阶段1并发全开 / 阶段2用户级限流）设计合理

**缺点：**
1. ReviewPlan ↔ reviewMode 双轨制是过渡期遗留，需逐步淘汰
2. 三层配置覆盖体系过度设计（静态→DB→pipeline_review_config→executionOverrides），4 层覆盖中后两层功能重叠
3. 存在可量化的死代码和弃用代码未清理（累计约 80+ 行）
4. CONSISTENCY 的"多项勾选=跨文件一致性"推导是错误的业务逻辑
