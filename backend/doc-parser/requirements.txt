# ============================================
# 文档解析服务
# MarkItDown 为主力解析引擎，增强解析器为 fallback
# 支持 DOCX / XLSX / PDF / PPTX
# DWG 已改为前端 WASM 解析（@mlightcad/libredwg-web），后端不再处理
# ============================================

# --- Web 框架 ---
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# --- MarkItDown: 统一文档转 Markdown 引擎（主力） ---
markitdown[all]>=0.1.0

# --- DOCX: python-docx 原生解析 ---
python-docx==1.1.2
lxml~=5.4.0
mammoth~=1.11.0

# --- XLSX: openpyxl 原生解析 ---
openpyxl~=3.1.5

# --- PDF: PyMuPDF 原生解析 + 内置表格提取 ---
PyMuPDF>=1.26.0
Pillow>=10.0.0

# --- OCR: RapidOCR 本地识别（轻量、离线、纯 CPU） ---
rapidocr>=3.9.0
onnxruntime>=1.20.0

# --- PPTX: python-pptx 原生解析 ---
python-pptx~=1.0.2

# --- 生产部署 ---
gunicorn~=23.0.0
