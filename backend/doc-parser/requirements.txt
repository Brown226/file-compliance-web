# ============================================
# 文档解析服务
# anydoc（Firecrawl，Rust）为主力解析引擎，增强解析器为 fallback
# 支持 DOC / DOCX / PPT / PPTX / XLS / XLSX / ODT / ODS / ODP / RTF / EPUB / CSV / PDF
# 扫描件 PDF 走 RapidOCR（本地）→ Vision LLM 兜底
# DWG 已改为前端 WASM 解析（@mlightcad/libredwg-web），后端不再处理
# 回退旧引擎：PARSER_ENGINE=markitdown（灰度/回滚用，需另行安装 markitdown）
# ============================================

# --- Web 框架 ---
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# --- anydoc: 统一文档转 Markdown 引擎（主力，Rust 实现） ---
firecrawl-anydoc>=0.1.8,<0.2

# --- PDF: PyMuPDF 仅用于 OCR 链路页面渲染（rapid_ocr.py） ---
PyMuPDF>=1.26.0
Pillow>=10.0.0

# --- OCR: RapidOCR 本地识别（轻量、离线、纯 CPU） ---
# opencv-python-headless 替代 opencv-python，避免 libGL.so.1 运行时缺失
# （RapidOCR 依赖 opencv，但 headless 版无需 GUI 图形库，适合容器）
opencv-python-headless>=4.9.0
rapidocr>=3.9.0
onnxruntime>=1.20.0

# --- 生产部署 ---
gunicorn~=23.0.0

# --- 已移除（anydoc 取代）：markitdown / python-docx / mammoth / lxml /
#     openpyxl / python-pptx / pdfplumber / antiword / LibreOffice ---
