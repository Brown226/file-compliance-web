# v2.0 架构地图（权威版）

> **本文档是项目唯一权威架构指导。后续所有开发、重构、功能增删必须依据本文档。**
>
> 更新日期：2026-06-07
> 基于代码实际状态编写，非目标架构。

---

## 一、产品定义

**v2.0 = 文件智能审查系统**

核心链路只有一条：

```
用户上传文件 → 系统解析内容 → 结合标准/规则/LLM 做审查 → 输出审查问题与整改建议
```

其他能力（知识库、智能问答、文档协作）均为这条主线的**增强辅助**，不能喧宾夺主。

---

## 二、模块分级

### 主线模块（核心，优先维护）

| 模块 | 说明 | 前端 | 后端 |
|------|------|------|------|
| 智能审查入口 | 上传→选方案→创建任务 | `SmartReview.vue` | `task.service` |
| 审查执行引擎 | 多管线+多规则的审查核心 | - | `review.service`, `review-pipeline/*`, `rules/*`, `rule-engine.service` |
| 审查结果展示 | 看问题、定位、导出、误报标记 | `TaskResultsView.vue` | `task.service` |
| 任务管理 | 历史列表、工作台统计 | `TaskHistory.vue`, `Workspace.vue` | `dashboard.service` |
| 标准库 | 企业标准维护 | `StandardLibrary/index.vue` | `standard.service`, `standardFolder.service` |
| 审查规则 | 结构化规则配置 | `ReviewRules.vue` | `rule.service` |
| 提示词模板 | LLM 提示词管理 | `PromptConfig.vue` | `prompt-template.service` |
| 系统管理 | 部门/员工/配置/审计 | `SystemManagement.vue`, `AuditLogs.vue` | `department.service`, `employee.service`, `audit.service`, `systemConfig.service` |

### 辅助模块（保留，不作为产品定义核心）

| 模块 | 说明 | 与主线关系 |
|------|------|-----------|
| 知识库管理 | MaxKB 知识库的文件夹/知识库浏览与管理 | 为审查提供 RAG 增强检索（通过 MaxKB hit_test API） |
| 智能问答 | iframe 嵌入 MaxKB 原生聊天界面 | 延展能力，非审查必需 |
| 规则库 | 结构化规则集管理 | 审查规则的补充管理方式 |
| 反馈/公告 | 用户反馈、系统公告 | 平台运营支撑 |

### 已清理模块

| 模块 | 清理时间 | 说明 |
|------|---------|------|
| ~~本地 pgvector 知识库~~ | 2026-06-07 | vector/document/knowledge-category 全链路已删除，知识库统一走 MaxKB |
| ~~LangChain QA~~ | 2026-06-06 | LangChainQA.vue 及 langchain/ 目录已删除，替换为 AiAssistant.vue iframe |
| ~~regex.routes~~ | 2026-05 | 前后端死代码已清理 |
| ~~TaskDetails/index.vue~~ | 2026-05 | 旧结果页已清理，子组件保留给 TaskResultsView |

---

## 三、审查核心架构（重点）

审查不是一条简单链路，而是一个**多管线+多规则**的复杂子系统。

### 3.1 审查管线（review-pipeline）

采用**工厂+策略模式**，4 种审查模式对应不同管线：

```
review-pipeline/
├── types.ts              ← 类型定义 (ReviewModeType, PipelineContext, ModeCapabilities)
├── mode-config.service.ts← 模式能力配置（DB 覆盖）
├── index.ts              ← 统一导出
│
├── review-handlers.ts    ← 各模式的审查处理器（编排逻辑）
│
├── text-extraction.service.ts   ← 文本提取服务（Parser/OCR/DWG/Word 结构化）
├── ai-review.service.ts         ← AI 审查服务（RAG/LLM/多模态策略）
├── standard-ref-check.service.ts← 标准引用检查服务（Prisma 访问在此）
└── pipeline-config.ts           ← 配置工具函数（纯函数，无外部依赖）
```

**两阶段执行模型**（review-handlers.ts）：

```
Phase 1（快速，并行）           Phase 2（慢速，AI）
┌─────────────────────┐       ┌─────────────────────┐
│ 文本提取 (Parser)    │       │ AI 审查 (LLM)       │
│ 规则引擎 (Rules)     │ ───→  │ 按模式选择 prompt    │
│ 标准引用检查         │       │ 流式返回结果         │
└─────────────────────┘       └─────────────────────┘
```

**已知架构债务**：
- review.service 与 review-handlers 之间存在 DWG 特殊逻辑重复
- 工厂的 5 秒内存缓存在并发场景下脆弱

### 3.2 规则引擎（rules/）

18 条结构化规则，独立于 LLM：

```
rules/
├── index.ts           ← RULE_REGISTRY（18条规则注册表）
├── types.ts           ← RuleCheckResult 类型
├── naming.rule.ts     ← 命名规范
├── encoding.rule.ts   ← 编码检查
├── attribute.rule.ts  ← 属性检查
├── header.rule.ts     ← 页眉检查
├── page.rule.ts       ← 页面规范
├── format.rule.ts     ← 格式检查
├── layout.rule.ts     ← 排版检查
├── typo.rule.ts       ← 错别字
├── consistency.rule.ts← 一致性
├── completeness.rule.ts← 完整性
├── internal-code.rule.ts← 内部编码
├── dwg.rule.ts        ← DWG 图纸
└── ...                ← 可扩展
```

规则引擎与管线通过 `ModeCapabilities.rulePrefixes` 解耦——不同审查模式启用不同规则子集。

### 3.3 审查编排（review.service）

```
ReviewService.processTask(taskId)
  │
  ├── 1. 加载任务/文件/配置（单次 DB 查询）
  ├── 2. 构建 PipelineContext（每文件一个）
  ├── 3. Phase 1 全文件并行（快速阶段）
  │     └── 立即持久化结果到 task_details
  ├── 4. Phase 2 并发控制（maxConcurrentReviews）
  │     └── 流式写入 + WebSocket 推送进度
  └── 5. 可选：跨文件一致性检查（cross-file-consistency.service）
```

---

## 四、知识库架构

### 当前实现：MaxKB（外部知识库服务）

知识库功能已从本地 pgvector 回退到 MaxKB 方案。MaxKB 提供完整的知识库管理（文档上传、分块、向量化、检索），后端通过 MaxKB API 进行集成。

| 组件 | 实现 | 说明 |
|------|------|------|
| **知识库管理** | MaxKB Web UI + `maxkb.service.ts` | 文件夹/知识库 CRUD，通过 MaxKB API |
| **文档上传** | MaxKB 原生 | 文档解析、分块、向量化均由 MaxKB 处理 |
| **向量检索** | `MaxKBService.hitTest()` | 调用 MaxKB hit_test API 获取相关段落 |
| **RAG 审查** | `rag.service.ts` | 检索增强生成，调用 MaxKB hit_test + 自有 LLM |
| **智能问答** | `AiAssistant.vue`（iframe） | 嵌入 MaxKB 原生聊天界面，保留对话历史 |
| **嵌入会话** | `maxkb-embed.service.ts` | 管理用户隔离的 chatUserToken 持久化 |

### 架构

```
┌──────────────────────────────────────────────────────┐
│  MaxKB 服务（独立部署）                                │
│                                                       │
│  知识库管理 / 文档上传 / 分块 / 向量化 / 检索          │
│  智能应用 / 聊天界面                                  │
│                                                       │
│  端口：8080                                           │
│  数据库：MaxKB PostgreSQL (:15432)                    │
│  缓存：MaxKB Redis (:16379)                           │
└───────────────────────┬──────────────────────────────┘
                        │ HTTP API
                        ▼
┌──────────────────────────────────────────────────────┐
│  后端集成层                                           │
│                                                       │
│  MaxKBService        ← API 客户端（CRUD + hit_test）  │
│  MaxKBEmbedService   ← iframe 嵌入会话管理            │
│  RAGService          ← RAG 审查编排（调用 hit_test）   │
│                                                       │
│  路由：/api/maxkb/*                                   │
└──────────────────────────────────────────────────────┘
```

### 前端入口

| 路由 | 组件 | 说明 |
|------|------|------|
| `/admin/knowledge` | `MaxKBTab.vue` | MaxKB 知识库管理（文件夹树 + 知识库列表） |
| `/ai-assistant` | `AiAssistant.vue` | iframe 嵌入 MaxKB 聊天界面 |
| 审查页面 | `KnowledgeTreeSelector.vue` | 知识库选择器（树形，用于审查时选择关联知识库） |

### 已删除的本地 pgvector 代码（2026-06-07）

以下文件已从代码库中移除，统一使用 MaxKB 方案：

- `vector.service.ts`（1236 行）— 文本分块 + pgvector 入库
- `embedding.service.ts` — 向量嵌入生成（保留，ai-review 仍用于参照向量索引）
- `knowledge-category.service.ts` — 本地知识分类 CRUD
- `knowledge-category.controller.ts` — 本地知识分类控制器
- `document.service.ts` — 本地文档管理
- `contextual-retrieval.service.ts` — 上下文增强检索
- `retrieval-evaluation.service.ts` — 检索质量评估
- `langchain/` 整个目录 — LangChain RAG/检索/适配器
- 前端 `KnowledgeCategories.vue`、`KnowledgeDocuments.vue`、`KnowledgeBaseSelector.vue`

---

## 五、服务依赖图

### 5.1 审查主线依赖

```
TaskService
  ├── QueueService ──→ ReviewService (异步)
  ├── ParserService (文件预览)
  └── FalsePositiveLibraryService

ReviewService（中枢，5+ 直接依赖）
  ├── review-pipeline/* (审查管线)
  ├── ParserService ──→ PythonParserService (MarkItDown)
  ├── LlmService ──→ PromptTemplateService
  ├── CrossFileConsistencyService ──→ ParserService + OcrService
  ├── WebSocketService (进度推送)
  └── ConcurrencyService (并发控制)

review-handlers（审查处理器）
  ├── TextExtractionService ──→ ParserService + OcrService
  ├── AiReviewService ──→ RAGService + LlmService + PromptTemplateService
  ├── StandardRefCheckService ──→ StandardExtractorService + StandardCheckService + CharDiffService + Prisma
  ├── pipeline-config (纯函数)
  └── RuleEngineService ──→ rules/*

RAGService（知识库检索）
  └── MaxKBService ──→ MaxKB HTTP API (hit_test)
```

### 5.2 独立服务（无内部 service 依赖）

| 服务 | 依赖 | 说明 |
|------|------|------|
| DocxReplaceService | adm-zip, path, fs | 完全独立 |
| VersionService | prisma, path, fs, uuid | 完全独立 |
| OcrService | prisma, fs | 完全独立 |
| FalsePositiveLibraryService | prisma | 完全独立 |
| WebSocketService | ws, prisma | 完全独立 |
| MaxKBService | axios | MaxKB API 客户端 |
| MaxKBEmbedService | prisma | iframe 嵌入会话管理 |

### 5.3 关键耦合点与改进方向

| 问题 | 位置 | 改进方向 |
|------|------|---------|
| DWG 特殊逻辑重复 | review.service + review-handlers | 提取到 DWG 专用处理器 |
| parse+OCR 回退重复 | review.service + cross-file-consistency | 提取为共享 text-extraction 工具（TextExtractionService 已可复用） |
| 工厂 5s 内存缓存 | review-pipeline/factory.ts | 改为 DB 配置直读或 Redis 缓存 |

---

## 六、前端架构

### 6.1 路由结构

```
/ (AppLayout)
├── /workspace          ← 工作台（所有角色）
├── /review             ← 智能审查入口（所有角色）
├── /review/:id         ← 审查结果（所有角色）★ 主力结果页
├── /tasks              ← 我的任务（所有角色）
├── /ai-assistant       ← 智能问答（iframe 嵌入 MaxKB）（所有角色）[辅助]
├── /feedback           ← 反馈意见（所有角色）[辅助]
├── /feedback/submit    ← 提交反馈（所有角色）[辅助]
├── /feedback/:id       ← 反馈详情（所有角色）[辅助]
├── /announcements      ← 系统公告（所有角色）[辅助]
│
├── /admin/dashboard    ← 数据看板（ADMIN/MANAGER）
├── /admin/standards    ← 标准库管理（ADMIN/MANAGER）
├── /admin/knowledge    ← MaxKB 知识库（ADMIN/MANAGER）[辅助]
├── /admin/rules        ← 审查规则（ADMIN/MANAGER）
├── /admin/prompts      ← 提示词模板（ADMIN/MANAGER）
├── /admin/rule-libraries       ← 规则库管理（ADMIN/MANAGER）[辅助]
├── /admin/system       ← 系统配置（ADMIN/MANAGER）
├── /admin/users        ← 部门与员工（ADMIN/MANAGER）→ 同 SystemManagement
├── /admin/audit        ← 审计日志（ADMIN/MANAGER）
└── /admin/feedback     ← 反馈管理（ADMIN/MANAGER）

遗留重定向：
  /tasks/details/:id  → /review/:id（已废弃）
  /dashboard          → /admin/dashboard
  /tasks/new          → /review
  /admin/knowledge-categories → /admin/knowledge
  /admin/knowledge-categories/:id/documents → /admin/knowledge
```

### 6.2 前端分层

```
views/          ← 页面组件（直接调用 api/ 模块，无中间 service 层）
api/            ← 16 个 API 模块，一一对应后端路由组
stores/         ← user.ts + system-config.ts
components/     ← 公共组件（AppLayout、KnowledgeTreeSelector 等）
utils/          ← request.ts（Axios 实例，/api 基础 URL，JWT 注入）
router/         ← 单文件路由，beforeEach 守卫
```

### 6.3 前端已知问题

| 问题 | 说明 |
|------|------|
| 单 store 架构 | 仅 user.ts + system-config.ts，所有业务状态散在各组件内部 |
| 视图直接调 API | 无 service/store 中间层，业务逻辑在 .vue 文件中 |
| SystemManagement 过载 | `/admin/system` 和 `/admin/users` 指向同一页面 |
| 超大 .vue 文件 | TaskResultsView(88KB)、Dashboard(49KB)、PromptConfig(47KB) 等需要拆分 |

---

## 七、数据模型

### Prisma Schema 模型清单

#### 核心模型（审查主线）

| 模型 | 表名 | 说明 |
|------|------|------|
| Department | departments | 部门树（自引用） |
| User | users | 用户/角色 |
| Standard | standards | 标准文档 |
| StandardFolder | standard_folders | 标准文件夹树 |
| Task | tasks | 审查任务 |
| TaskFile | task_files | 任务关联文件 |
| TaskDetail | task_details | 审查结果明细 |
| TaskStandard | task_standards | 任务-标准 M:M |
| ReviewRule | review_rules | 审查规则（DB 存储） |
| PromptTemplate | prompt_templates | 提示词模板 |

#### 知识库模型（遗留，本地 pgvector 已停用）

| 模型 | 表名 | 说明 | 状态 |
|------|------|------|------|
| KnowledgeCategory | knowledge_categories | 知识分类 | ⚠️ 遗留，运行时不使用 |
| VectorDocument | vector_documents | 向量文档/段落 | ⚠️ 遗留，运行时不使用 |
| Tag | tags | 标签 | ⚠️ 遗留，运行时不使用 |
| DocumentTag | document_tags | 文档-标签 M:M | ⚠️ 遗留，运行时不使用 |

> 注：这些 Prisma 模型仍保留在 schema 中，但后端已无代码访问它们。后续可安全移除。

#### 规则库模型

| 模型 | 表名 | 说明 |
|------|------|------|
| RuleLibrary | rule_libraries | 规则库 |
| RuleLibraryItem | rule_library_items | 规则条目 |

#### 辅助模型

| 模型 | 表名 | 说明 |
|------|------|------|
| Feedback | feedbacks | 用户反馈 |
| SystemAnnouncement | system_announcements | 系统公告 |
| UserAnnouncementRead | user_announcement_reads | 公告已读状态 |
| UserChatSession | user_chat_sessions | MaxKB 嵌入会话 token（按 userId+applicationId） |
| AuditLog | audit_logs | 审计日志 |
| SystemConfig | system_configs | 运行时配置（LLM/OCR/Embedding 等） |
| FalsePositiveLibrary | false_positive_library | 误报库 |
| TerminologyWhitelist | terminology_whitelist | 术语白名单 |
| FileVersion | file_versions | 文件版本 |
| RefFileGroup / RefFile | ref_file_groups / ref_files | 引用文件组 |
| TempStandardEntry | temp_standard_entries | 临时标准条目 |

#### 已删除的模型

| 模型 | 说明 |
|------|------|
| ~~QASession / QAMessage~~ | 旧 LangChain QA 会话，随 LangChainQA.vue 一起清理 |

---

## 八、外部服务依赖

### Docker 服务清单

| 服务 | 镜像/端口 | 用途 | 必需 |
|------|----------|------|------|
| PostgreSQL 15 | pgvector/pgvector:pg15 / 5432 | 主数据库 | 是 |
| Redis 7 | redis:7-alpine / 6379 | BullMQ 队列+缓存 | 是 |
| MarkItDown | file-review-markitdown / 8000 | Python 文档解析 | 是 |
| PaddleOCR-VL | ocr-service / 8001 | OCR 回退 | 是 |
| **MaxKB** | maxkb / 8080 | **知识库服务（唯一实现）** | **是** |
| MaxKB PostgreSQL | maxkb-pgsql / 15432 | MaxKB 专用数据库 | 是（MaxKB 依赖） |
| MaxKB Redis | maxkb-redis / 16379 | MaxKB 专用缓存 | 是（MaxKB 依赖） |

### 外部 API

| 服务 | 配置来源 | 说明 |
|------|---------|------|
| LLM API | SystemConfig DB 表 | OpenAI 兼容接口，运行时可切换 |
| Embedding API | SystemConfig DB 表 | 向量嵌入服务（ai-review 参照向量索引使用） |

---

## 九、开发规范

### 9.1 解耦原则

1. **服务间通过接口交互，不直接 import 实现细节**
   - 错误：`BasePipeline` 直接 `new PrismaClient()` 查询
   - 正确：通过 `PipelineContext` 传入所需数据

2. **新增功能必须评估影响范围**
   - 修改 `review.service` 前检查所有调用方
   - 修改 `review-handlers` 前检查所有模式处理器

3. **辅助模块不得在主线服务中被直接依赖**
   - 知识库/问答等辅助模块的 service 不应被 review.service 直接 import
   - 如需集成，通过配置开关 + 可选调用（try-catch 降级）

4. **每个模块必须有清晰的入口和出口**
   - 入口：route → controller → service
   - 出口：返回值/DB 写入/WebSocket 推送，三选一，不混用

### 9.2 新增功能检查清单

- [ ] 是否属于主线？是则需要更高审查标准
- [ ] 是否引入新的 service 依赖？检查是否造成循环
- [ ] 是否需要新路由？前后端 API 模块是否对应
- [ ] 是否需要新 DB 模型？检查是否与现有模型职责重叠
- [ ] 是否需要外部服务？检查 docker-compose 是否需要更新
- [ ] 是否有配置开关？确保可以降级/关闭

### 9.3 禁止事项

- **禁止**在 review.service 中直接 import 知识库/问答等辅助模块的 service
- **禁止**在前端组件中直接操作 localStorage（通过 store）
- **禁止**在 service 中直接 `new PrismaClient()`（使用共享单例 `config/db.ts`）
- **禁止**新增 god-class（单文件超过 500 行必须拆分）
- **禁止**在路由文件中写业务逻辑（路由只做路径映射+中间件组合）

---

## 十、技术债清单（2026-06-07 更新）

按优先级排序，每项已验证：

| 优先级 | 项目 | 位置 | 状态 | 影响 |
|--------|------|------|------|------|
| ~~P0~~ | ~~BasePipeline god-class 拆分~~ | ~~review-pipeline/base-pipeline.ts~~ | **已修复** | 已解决 |
| ~~P0~~ | ~~本地 pgvector 知识库代码残留~~ | ~~vector/document/knowledge-category 全链路~~ | **已清理**（2026-06-07） | 删除 29 个文件，12,302 行 |
| ~~P0~~ | ~~LangChain QA 死代码~~ | ~~langchain/ 目录 + LangChainQA.vue~~ | **已清理**（2026-06-06） | 替换为 AiAssistant.vue iframe |
| P1 | task.service 违规 import | task.service.ts:8 | 保留 | FalsePositiveLibraryService 直接导入（评估为合理：误报标记是审查工作流的一部分） |
| P2 | 遗留 Prisma 模型 | schema.prisma | 待清理 | KnowledgeCategory/VectorDocument/Tag/DocumentTag 模型仍存在但无代码访问 |
| P2 | SystemManagement 拆分 | views/SystemManagement.vue | 待处理 | /admin/system 和 /admin/users 指向同一组件 |
| P3 | maxkbKnowledgeIds 字段语义 | task.service, review-pipeline/types | 保留 | 字段名含"maxkb"，现确存储 MaxKB 知识库 ID，语义已正确 |
| P3 | 前端增加 service/store 层 | views → api | 待处理 | 视图直接调 API，无中间层 |
| P3 | 工厂缓存改为 Redis | review-pipeline/factory.ts | 待处理 | 5秒内存缓存，并发场景脆弱 |
| P3 | 超大 .vue 文件 | TaskResultsView(88KB)、Dashboard(49KB) 等 | 待拆分 | 业务逻辑全内联在 .vue 文件 |
| P3 | 415 处 `: any` 类型 | 后端 src/ | 待优化 | TypeScript 类型安全名存实亡 |
| P3 | 无单元测试 | backend/package.json | 待补充 | npm test = echo "Error: no test specified" |

---

## 十一、部署架构

### 开发环境

```
docker-compose up -d          → PostgreSQL + Redis
npm run dev (backend/)        → 后端 :3000
npm run dev (frontend/)       → Vite :5173 → proxy /api → :3000
```

> 注：开发环境如需知识库/问答功能，需额外启动 MaxKB 服务。

### 离线/生产环境

```
docker-compose.offline.yml    → 全栈部署
  ├── postgres (:5432)
  ├── redis (:6379)
  ├── markitdown (:8000)
  ├── ocr-service (:8001)
  ├── maxkb (:8080)          ← 知识库 + 智能问答
  ├── maxkb-pgsql (:15432)   ← MaxKB 专用数据库
  ├── maxkb-redis (:16379)   ← MaxKB 专用缓存
  ├── backend (:3000)
  └── frontend (:80)
```

### 单独 OCR 服务

```
docker-compose.ocr.yml        → 仅 PaddleOCR-VL
  └── ocr-service (:8001)
```

---

## 十二、关键变更历史

| 日期 | 变更 | 说明 |
|------|------|------|
| 2026-06-07 | 清理 pgvector 死代码 | 删除 29 个文件（12,302 行），知识库统一走 MaxKB |
| 2026-06-06 | 智能问答迁移到 MaxKB iframe | 删除 LangChainQA.vue（1257 行），新增 AiAssistant.vue |
| 2026-06-05 | 知识库从 pgvector 回退到 MaxKB | 审查+问答全链路切换到 MaxKB |
| 2026-05-12 | 首次架构审计 | BasePipeline god-class 拆分、技术债清单建立 |
