# v2.0 架构地图（权威版）

> **本文档是项目唯一权威架构指导。后续所有开发、重构、功能增删必须依据本文档。**
>
> 更新日期：2026-05-12
> 基于代码实际状态编写，非目标架构。

---

## 一、产品定义

**v2.0 = 文件智能审查系统**

核心链路只有一条：

```
用户上传文件 → 系统解析内容 → 结合标准/规则/LLM 做审查 → 输出审查问题与整改建议
```

其他能力（知识库、智能问答、文档协作）均为这条主线的**增强辅助**，不能喧宾夺主。

---

## 二、模块分级

### 主线模块（核心，优先维护）

| 模块 | 说明 | 前端 | 后端 |
|------|------|------|------|
| 智能审查入口 | 上传→预分析→选方案→创建任务 | `SmartReview.vue` | `task.service`, `pre-analysis.service` |
| 审查执行引擎 | 多管线+多规则的审查核心 | - | `review.service`, `review-pipeline/*`, `rules/*`, `rule-engine.service` |
| 审查结果展示 | 看问题、定位、导出、误报标记 | `TaskResultsView.vue` | `task.service` |
| 任务管理 | 历史列表、工作台统计 | `TaskHistory.vue`, `Workspace.vue` | `dashboard.service` |
| 标准库 | 企业标准维护 | `StandardLibrary/index.vue` | `standard.service`, `standardFolder.service` |
| 审查规则 | 结构化规则配置 | `ReviewRules.vue` | `rule.service` |
| 提示词模板 | LLM 提示词管理 | `PromptConfig.vue` | `prompt-template.service` |
| 系统管理 | 部门/员工/配置/审计 | `SystemManagement.vue`, `AuditLogs.vue` | `department.service`, `employee.service`, `audit.service`, `systemConfig.service` |

### 辅助模块（保留，不作为产品定义核心）

| 模块 | 说明 | 与主线关系 |
|------|------|-----------|
| 知识库管理 | 分类/文档/向量化/命中测试 | 为审查提供 RAG 增强检索 |
| 智能问答 | 基于知识库的问答 | 延展能力，非审查必需 |
| 规则库 | 结构化规则集管理 | 审查规则的补充管理方式 |
| 反馈/公告 | 用户反馈、系统公告 | 平台运营支撑 |

### 遗留模块（待清理）

| 模块 | 状态 | 处理方式 |
|------|------|---------|
| `TaskDetails/` 旧结果页 | 路由已 redirect 到 `/review/:id` | 代码保留但不再维护，后续删除 |
| `regex.routes` | 后端路由仍注册 | 待确认是否仍有调用方，无则删除 |
| 旧文档残留 | `AGENTS.md`/`CONTEXT.md` 描述与代码不一致 | 以本文档为准，后续清理旧文档 |

---

## 三、审查核心架构（重点）

审查不是一条简单链路，而是一个**多管线+多规则**的复杂子系统。

### 3.1 审查管线（review-pipeline）

采用**工厂+策略模式**，4 种审查模式对应不同管线：

```
review-pipeline/
├── types.ts              ← 类型定义 (ReviewModeType, PipelineContext, ModeCapabilities)
├── base-pipeline.ts      ← 抽象编排器 (417行，两阶段编排+委托)
├── factory.ts            ← 工厂，按模式创建管线实例
├── mode-config.ts        ← 模式能力配置（静态）
├── mode-config.service.ts← 模式能力配置（DB 覆盖）
├── index.ts              ← 统一导出
│
├── standard.pipeline.ts  ← 标准审查（LIBRARY_REVIEW / CONSISTENCY / FULL_REVIEW / CUSTOM_RULE）
├── doc-review.pipeline.ts← 文档审查（DOC_REVIEW，引用文件对比）
├── typo-grammar.pipeline.ts ← 错别字/语法（TYPO_GRAMMAR，术语白名单过滤）
├── multimodal.pipeline.ts   ← 多模态（MULTIMODAL，表格/公式检测+视觉LLM）
│
├── text-extraction.service.ts   ← 文本提取服务（Parser/OCR/DWG/Word 结构化）
├── ai-review.service.ts         ← AI 审查服务（RAG/LLM/多模态策略）
├── standard-ref-check.service.ts← 标准引用检查服务（Prisma 访问在此）
└── pipeline-config.ts           ← 配置工具函数（纯函数，无外部依赖）
```

**两阶段执行模型**（BasePipeline）：

```
Phase 1（快速，并行）           Phase 2（慢速，AI）
┌─────────────────────┐       ┌─────────────────────┐
│ 文本提取 (Parser)    │       │ AI 审查 (LLM)       │
│ 规则引擎 (Rules)     │ ───→  │ 按模式选择 prompt    │
│ 标准引用检查         │       │ 流式返回结果         │
└─────────────────────┘       └─────────────────────┘
```

**已知架构债务**：
- ~~BasePipeline 是 god-class~~ → 已拆分为 4 个 service 模块，BasePipeline 降至 417 行
- review.service 与 BasePipeline 之间存在 DWG 特殊逻辑重复
- 工厂的 5 秒内存缓存在并发场景下脆弱

### 3.2 规则引擎（rules/）

18 条结构化规则，独立于 LLM：

```
rules/
├── index.ts           ← RULE_REGISTRY（18条规则注册表）
├── types.ts           ← RuleCheckResult 类型
├── naming.rule.ts     ← 命名规范
├── encoding.rule.ts   ← 编码检查
├── attribute.rule.ts  ← 属性检查
├── header.rule.ts     ← 页眉检查
├── page.rule.ts       ← 页面规范
├── format.rule.ts     ← 格式检查
├── layout.rule.ts     ← 排版检查
├── typo.rule.ts       ← 错别字
├── consistency.rule.ts← 一致性
├── completeness.rule.ts← 完整性
├── internal-code.rule.ts← 内部编码
├── dwg.rule.ts        ← DWG 图纸
└── ...                ← 可扩展
```

规则引擎与管线通过 `ModeCapabilities.rulePrefixes` 解耦——不同审查模式启用不同规则子集。

### 3.3 审查编排（review.service）

```
ReviewService.processTask(taskId)
  │
  ├── 1. 加载任务/文件/配置（单次 DB 查询）
  ├── 2. 构建 PipelineContext（每文件一个）
  ├── 3. Phase 1 全文件并行（快速阶段）
  │     └── 立即持久化结果到 task_details
  ├── 4. Phase 2 并发控制（maxConcurrentReviews）
  │     └── 流式写入 + WebSocket 推送进度
  └── 5. 可选：跨文件一致性检查（cross-file-consistency.service）
```

---

## 四、知识库架构

### 当前实现：单轨（本地 pgvector）

| 组件 | 实现 | 说明 |
|------|------|------|
| **向量存储** | pgvector（PostgreSQL 扩展） | 本地存储，无外部依赖 |
| **文本分块** | `vector.service.ts` | 移植自 MaxKB 2.8.0 的 SplitModel，按 Markdown 标题层级分块 |
| **向量嵌入** | `embedding.service.ts` | 调用外部 Embedding API（OpenAI 兼容） |
| **混合检索** | `search.service.ts` | 三阶段：向量 + 关键词 + Rerank |
| **RAG 审查** | `rag.service.ts` | 检索增强生成，为审查提供知识库上下文 |
| **知识分类** | `knowledge-category.service.ts` | CRUD 管理，纯数据层 |

### 架构

```
KnowledgeCategoryService (CRUD 数据层)
        │
        ▼
┌─────────────────────────────────────────┐
│  本地 RAG 路线（唯一实现）                │
│                                          │
│  vector.service     ← 文本分块+入库      │
│  embedding.service  ← 向量嵌入生成       │
│  search.service     ← 混合检索           │
│  rag.service        ← RAG 审查编排       │
│                                          │
│  存储：pgvector (PostgreSQL)             │
│  外部依赖：Embedding API                 │
└─────────────────────────────────────────┘
```

### MaxKB 现状

MaxKB 2.8.0 仍在 `docker-compose.offline.yml` 中部署（含独立 PostgreSQL/Redis），但**后端代码中无任何 MaxKB API 调用**。所有 `maxkb*` 引用均为遗留：

- **字段名遗留**：`maxkbKnowledgeId` / `maxkbKnowledgeIds` 实际存储的是本地 knowledge category ID
- **注释遗留**：vector.service.ts 中"移植自 MaxKB 2.8.0"等注释仅为算法出处说明
- **配置遗留**：`.env` 中 `MAXKB_BASE_URL` 为孤立变量，无代码读取
- **管线遗留**：`maxkbTimeout` 字段标记为"兼容旧配置（已废弃）"

**结论**：如需启用 MaxKB 路线，需从零实现 API 客户端和切换机制。当前所有知识库功能完全依赖本地 pgvector。

---

## 五、服务依赖图

### 5.1 审查主线依赖

```
TaskService
  ├── QueueService ──→ ReviewService (异步)
  ├── ParserService (文件预览)
  └── FalsePositiveLibraryService

ReviewService（中枢，5+ 直接依赖）
  ├── review-pipeline/* (审查管线)
  ├── ParserService ──→ PythonParserService (MarkItDown)
  ├── LlmService ──→ PromptTemplateService
  ├── CrossFileConsistencyService ──→ ParserService + OcrService
  ├── WebSocketService (进度推送)
  └── ConcurrencyService (并发控制)

BasePipeline（编排器，417行，4 依赖）
  ├── TextExtractionService ──→ ParserService + OcrService
  ├── AiReviewService ──→ RAGService + VectorService + LlmService + PromptTemplateService + StandardTraceabilityService
  ├── StandardRefCheckService ──→ StandardExtractorService + StandardCheckService + CharDiffService + Prisma
  ├── pipeline-config (纯函数)
  └── RuleEngineService ──→ rules/*
```

### 5.2 独立服务（无内部 service 依赖）

| 服务 | 依赖 | 说明 |
|------|------|------|
| DocxReplaceService | adm-zip, path, fs | 完全独立 |
| VersionService | prisma, path, fs, uuid | 完全独立 |
| OcrService | prisma, fs | 完全独立 |
| FalsePositiveLibraryService | prisma | 完全独立 |
| WebSocketService | ws, prisma | 完全独立 |
| UploadTaskService | 无（内存 Map） | 完全独立 |

### 5.3 关键耦合点与改进方向

| 问题 | 位置 | 改进方向 |
|------|------|---------|
| ~~BasePipeline god-class~~ | ~~review-pipeline/base-pipeline.ts~~ | **已修复**：拆为 4 个 service 模块 |
| DWG 特殊逻辑重复 | review.service + base-pipeline | 提取到 DWG 专用处理器 |
| parse+OCR 回退重复 | review.service + cross-file-consistency | 提取为共享 text-extraction 工具（TextExtractionService 已可复用） |
| 工厂 5s 内存缓存 | review-pipeline/factory.ts | 改为 DB 配置直读或 Redis 缓存 |

---

## 六、前端架构

### 6.1 路由结构

```
/ (AppLayout)
├── /workspace          ← 工作台（所有角色）
├── /review             ← 智能审查入口（所有角色）
├── /review/:id         ← 审查结果（所有角色）★ 主力结果页
├── /tasks              ← 我的任务（所有角色）
├── /qna                ← 智能问答（所有角色）[辅助]
├── /feedback           ← 反馈意见（所有角色）[辅助]
├── /feedback/submit    ← 提交反馈（所有角色）[辅助]
├── /feedback/:id       ← 反馈详情（所有角色）[辅助]
├── /announcements      ← 系统公告（所有角色）[辅助]
│
├── /admin/dashboard    ← 数据看板（ADMIN/MANAGER）
├── /admin/standards    ← 标准库管理（ADMIN/MANAGER）
├── /admin/rules        ← 审查规则（ADMIN/MANAGER）
├── /admin/prompts      ← 提示词模板（ADMIN/MANAGER）
├── /admin/knowledge-categories ← 知识库管理（ADMIN/MANAGER）[辅助]
├── /admin/rule-libraries       ← 规则库管理（ADMIN/MANAGER）[辅助]
├── /admin/system       ← 系统配置（ADMIN/MANAGER）
├── /admin/users        ← 部门与员工（ADMIN/MANAGER）→ 同 SystemManagement
├── /admin/audit        ← 审计日志（ADMIN/MANAGER）
└── /admin/feedback     ← 反馈管理（ADMIN/MANAGER）

遗留重定向：
  /tasks/details/:id  → /review/:id（已废弃）
  /dashboard          → /admin/dashboard
  /tasks/new          → /review
```

### 6.2 前端分层

```
views/          ← 页面组件（直接调用 api/ 模块，无中间 service 层）
api/            ← 18 个 API 模块，一一对应后端路由组
stores/         ← 仅 user.ts（token + userInfo + 角色判断）
components/     ← 公共组件（AppLayout、PreviewConfirmDialog 等）
utils/          ← request.ts（Axios 实例，/api 基础 URL，JWT 注入）
router/         ← 单文件路由，beforeEach 守卫
```

### 6.3 前端已知问题

| 问题 | 说明 |
|------|------|
| 单 store 架构 | 仅 user.ts，所有业务状态散在各组件内部 |
| 视图直接调 API | 无 service/store 中间层，业务逻辑在 .vue 文件中 |
| SystemManagement 过载 | `/admin/system` 和 `/admin/users` 指向同一页面 |

---

## 七、数据模型

### Prisma Schema 模型清单（28 个）

#### 核心模型（审查主线）

| 模型 | 表名 | 说明 |
|------|------|------|
| Department | departments | 部门树（自引用） |
| User | users | 用户/角色 |
| Standard | standards | 标准文档 |
| StandardFolder | standard_folders | 标准文件夹树 |
| Task | tasks | 审查任务 |
| TaskFile | task_files | 任务关联文件 |
| TaskDetail | task_details | 审查结果明细 |
| TaskStandard | task_standards | 任务-标准 M:M |
| ReviewRule | review_rules | 审查规则（DB 存储） |
| PromptTemplate | prompt_templates | 提示词模板 |

#### 知识库模型

| 模型 | 表名 | 说明 |
|------|------|------|
| KnowledgeCategory | knowledge_categories | 知识分类 |
| VectorDocument | vector_documents | 向量文档/段落 |
| Tag | tags | 标签 |
| DocumentTag | document_tags | 文档-标签 M:M |

#### 规则库模型

| 模型 | 表名 | 说明 |
|------|------|------|
| RuleLibrary | rule_libraries | 规则库 |
| RuleLibraryItem | rule_library_items | 规则条目 |

#### 辅助模型

| 模型 | 表名 | 说明 |
|------|------|------|
| Feedback | feedbacks | 用户反馈 |
| SystemAnnouncement | system_announcements | 系统公告 |
| UserAnnouncementRead | user_announcement_reads | 公告已读状态 |
| QASession | qa_sessions | 问答会话 |
| QAMessage | qa_messages | 问答消息 |
| AuditLog | audit_logs | 审计日志 |
| SystemConfig | system_configs | 运行时配置（LLM/OCR/知识库模式等） |
| FalsePositiveLibrary | false_positive_library | 误报库 |
| TerminologyWhitelist | terminology_whitelist | 术语白名单 |
| FileVersion | file_versions | 文件版本 |
| RefFileGroup / RefFile | ref_file_groups / ref_files | 引用文件组 |
| TempStandardEntry | temp_standard_entries | 临时标准条目 |

---

## 八、外部服务依赖

### Docker 服务清单

| 服务 | 镜像/端口 | 用途 | 必需 |
|------|----------|------|------|
| PostgreSQL 15 (pgvector) | pgvector/pgvector:pg15 / 5432 | 主数据库+向量存储 | 是 |
| Redis 7 | redis:7-alpine / 6379 | BullMQ 队列+缓存 | 是 |
| MarkItDown | file-review-markitdown / 8000 | Python 文档解析 | 是 |
| PaddleOCR-VL | ocr-service / 8001 | OCR 回退 | 是 |
| MaxKB 2.8.0 | maxkb / 8080 | 知识库服务（路线 A） | 否（可选） |
| MaxKB PostgreSQL | maxkb-pgsql / 15432 | MaxKB 专用数据库 | 否（MaxKB 依赖） |
| MaxKB Redis | maxkb-redis / 16379 | MaxKB 专用缓存 | 否（MaxKB 依赖） |

### 外部 API

| 服务 | 配置来源 | 说明 |
|------|---------|------|
| LLM API | SystemConfig DB 表 | OpenAI 兼容接口，运行时可切换 |
| Embedding API | SystemConfig DB 表 | 向量嵌入服务 |

---

## 九、开发规范

### 9.1 解耦原则

1. **服务间通过接口交互，不直接 import 实现细节**
   - 错误：`BasePipeline` 直接 `new PrismaClient()` 查询
   - 正确：通过 `PipelineContext` 传入所需数据

2. **新增功能必须评估影响范围**
   - 修改 `review.service` 前检查所有调用方
   - 修改 `BasePipeline` 前检查所有 4 个管线子类

3. **辅助模块不得在主线服务中被直接依赖**
   - 知识库/问答等辅助模块的 service 不应被 review.service 直接 import
   - 如需集成，通过配置开关 + 可选调用（try-catch 降级）

4. **每个模块必须有清晰的入口和出口**
   - 入口：route → controller → service
   - 出口：返回值/DB 写入/WebSocket 推送，三选一，不混用

### 9.2 新增功能检查清单

- [ ] 是否属于主线？是则需要更高审查标准
- [ ] 是否引入新的 service 依赖？检查是否造成循环
- [ ] 是否需要新路由？前后端 API 模块是否对应
- [ ] 是否需要新 DB 模型？检查是否与现有模型职责重叠
- [ ] 是否需要外部服务？检查 docker-compose 是否需要更新
- [ ] 是否有配置开关？确保可以降级/关闭

### 9.3 禁止事项

- **禁止**在 review.service 中直接 import 知识库/问答等辅助模块的 service
- **禁止**在前端组件中直接操作 localStorage（通过 store）
- **禁止**在 service 中直接 `new PrismaClient()`（使用共享单例 `config/db.ts`）
- **禁止**新增 god-class（单文件超过 500 行必须拆分）
- **禁止**在路由文件中写业务逻辑（路由只做路径映射+中间件组合）

---

## 十、技术债清单（2026-05-12 审计结果）

按优先级排序，每项已验证：

| 优先级 | 项目 | 位置 | 审计状态 | 影响 |
|--------|------|------|---------|------|
| ~~P0~~ | ~~BasePipeline god-class 拆分~~ | ~~review-pipeline/base-pipeline.ts~~ | **已修复**：975行→417行，拆为 4 个 service 模块 | 已解决 |
| ~~P0~~ | ~~BasePipeline 直接访问 Prisma~~ | ~~base-pipeline.ts:748~~ | **已修复**：Prisma 访问移至 StandardRefCheckService | 已解决 |
| ~~P1~~ | ~~BasePipeline 辅助模块违规 import~~ | ~~base-pipeline.ts:27-34~~ | **已修复**：6 个辅助 import 移至提取的 service | 已解决 |
| P1 | task.service 违规 import | task.service.ts:7 | FalsePositiveLibraryService 直接导入 | 主线服务依赖辅助模块（评估后认为合理：误报标记是审查工作流的一部分） |
| P2 | regex 死代码清理 | routes/regex.routes.ts + api/regex.ts | 前端 API 模块存在但无任何文件导入，后端路由注册但无调用方 | 代码冗余 |
| P2 | TaskDetails/index.vue 清理 | views/TaskDetails/index.vue | 无路由加载此页面，但子组件（DwgPreviewPanel、TextPreviewPanel、FalsePositiveDialog）仍被 TaskResultsView 使用 | 需删除 index.vue，保留子组件目录 |
| P2 | SystemManagement 拆分 | views/SystemManagement.vue | /admin/system 和 /admin/users 指向同一组件 | 承载过多职责，tab 切换可能不触发 remount |
| P3 | maxkbKnowledgeId 字段重命名 | task.service, task.controller, review-pipeline/types, review.service | 字段名含"maxkb"但实际存储本地 knowledge category ID | 命名误导 |
| P3 | 前端增加 service/store 层 | views → api | 视图直接调 API，无中间层 | 业务逻辑散在 .vue 文件中 |
| P3 | 工厂缓存改为 Redis | review-pipeline/factory.ts | 5秒内存缓存，并发场景脆弱 | 并发请求可能读到过期配置 |
| P3 | 旧文档清理 | AGENTS.md, CONTEXT.md | 与代码不一致 | 以 v2.0架构.md 为准 |

---

## 十一、部署架构

### 开发环境

```
docker-compose up -d          → PostgreSQL + Redis
npm run dev (backend/)        → 后端 :3000
npm run dev (frontend/)       → Vite :5173 → proxy /api → :3000
```

### 离线/生产环境

```
docker-compose.offline.yml    → 全栈部署
  ├── postgres (:5432)
  ├── redis (:6379)
  ├── markitdown (:8000)
  ├── ocr-service (:8001)
  ├── maxkb (:8080) [可选]
  ├── backend (:3000)
  └── frontend (:80)
```

### 单独 OCR 服务

```
docker-compose.ocr.yml        → 仅 PaddleOCR-VL
  └── ocr-service (:8001)
```

---

## 十二、与旧架构文档的差异说明

| 旧文档描述 | 实际代码状态（2026-05-12 审计） | 本文档处理 |
|-----------|------------|----------|
| "MaxKB 已不在服务层中" | docker-compose 中仍部署，但后端无任何 MaxKB API 调用，无切换机制 | 修正为"单轨 pgvector，MaxKB 仅基础设施残留" |
| "43 个后端服务文件" | 实际 66 个（含 rules/ 11 个 + review-pipeline/ 8 个） | 本文档完整覆盖 |
| "PromptTemplate schema 中未显式定义" | 已存在于 schema.prisma 第 314 行 | 修正 |
| 未提及 review-pipeline 子系统 | 审查核心已拆为 4 种管线，BasePipeline 975行 god-class | 本文档重点描述 + P0 技术债 |
| 未提及 rules/ 模块化规则 | 18 条独立规则 | 本文档完整覆盖 |
| "regex.routes 残留待确认" | 审计确认：死代码，前端 API 无任何文件导入 | 已清理 |
| "TaskDetails 旧页面仍在路由中" | 审计确认：路由已 redirect，index.vue 可删，子组件仍被 TaskResultsView 使用 | 已清理 index.vue，保留子组件 |
| 未提及 BasePipeline 违规 import | 6 个辅助模块违规 import（RAG、Vector、StandardTraceability 等） | 记录为 P1 技术债 |
| 未提及 task.service 违规 import | FalsePositiveLibraryService 直接导入 | 记录为 P1 技术债 |
