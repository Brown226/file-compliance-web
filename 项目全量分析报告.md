# 文件智能审查系统 — 全量分析报告

> 分析日期：2026-05-23 | 分支：v2.0 | 分析范围：全量代码库

---

## 一、架构总览

### 1.1 系统定位

文件合规审查系统是企业内部文档智能审查平台，支持上传业务文档（DOCX/XLSX/PDF/PPTX/DWG），自动依据国家标准库、审查规范集、用户自定义规则，结合 LLM 大模型进行多维度合规性检查。

### 1.2 技术栈全景

```
┌─────────────────────────────────────────────────┐
│                   前端 (Vite 6)                   │
│  Vue 3 + Pinia + Element Plus + ECharts + TS    │
│  端口: 5173 (dev) / Nginx:80 (prod)             │
├─────────────────────────────────────────────────┤
│                 Nginx 反向代理                    │
│  静态资源缓存 + /api/ 代理 + /ws WebSocket      │
├──────────────────┬──────────────┬───────────────┤
│  Node.js 后端     │ MarkItDown   │ OCR Service   │
│  Express 5       │ FastAPI:8000 │ FastAPI:8001   │
│  Prisma 5 ORM    │ Python 3.12  │ PaddleOCR 3.5  │
│  端口: 3000      │ 文档解析      │ 图片OCR        │
├──────────────────┴──────────────┴───────────────┤
│            PostgreSQL 15  +  Redis 7             │
│      pgvector 向量扩展 (4096维嵌入)              │
└─────────────────────────────────────────────────┘
```

### 1.3 核心数据流

```
用户上传文件 → TaskService.createTask()
  → 文件保存到 backend/uploads/
  → 异步触发 ReviewService.processTask(taskId) [通过 addReviewJob 入队]
    → 阶段1 (runFastPhase): 并行规则审查
      → ParserService → MarkItDown 解析文本
      → OcrService (PDF扫描件兜底)
      → DWG: 前端 WASM 预填充数据
      → 规则引擎: 命名/编码/属性/页眉/页码/扫描件检测
      → 标准引用规范检查
      → 结果写入 task_details + WebSocket 推送
    → 阶段2 (runSlowPhase): AI 审查 (受并发控制)
      → PipelineFactory 创建对应 Pipeline
      → AiReviewService: RAG检索 → LLM审查
      → 分片级进度推送 (WebSocket)
      → 结果写入 task_details + WebSocket 推送
    → 跨文件一致性检查
    → 任务状态 → COMPLETED + WebSocket 最终推送
  → 前端 TaskResultsView 实时展示审查结果
```

### 1.4 目录结构 (核心模块)

```
file-compliance-web/
├── frontend/src/              # Vue3 前端 (60+ Vue 组件)
│   ├── api/                   # 20 个 API 模块 (Axios 封装)
│   ├── stores/user.ts         # 唯一的 Pinia Store (JWT + 用户信息)
│   ├── router/index.ts        # Vue Router (认证守卫)
│   ├── utils/                 # request.ts (Axios) / sse.ts / dwg-parser.ts
│   ├── composables/           # 11 个复用组合函数
│   ├── views/                 # 页面组件 (SmartReview/TaskResultsView 等)
│   ├── components/            # 全局组件 (AppLayout/DwgPreview 等)
│   └── styles/                # CSS 变量 + Element Plus 覆盖
│
├── backend/src/               # Node.js 后端
│   ├── app.ts                 # Express 入口 (36条路由注册)
│   ├── index.ts               # 服务启动 (WebSocket + 优雅关闭)
│   ├── config/                # db.ts (Prisma单例) / env.ts
│   ├── middlewares/           # 4 个中间件 (auth/rbac/audit/error)
│   ├── routes/                # 25 个路由模块
│   ├── controllers/           # 21 个控制器
│   └── services/              # 58 个服务文件
│       ├── review-pipeline/   # 审查管线 (14 文件)
│       ├── rules/             # 规则引擎 (13 条规则)
│       ├── langchain/         # RAG/LangChain 集成 (5 文件)
│       └── common/jieba.ts    # 结巴分词
│
├── backend/markitdown-service/# Python 文档解析微服务
├── backend/ocr-service/       # PaddleOCR 微服务
├── backend/prisma/            # 数据库 Schema (28 模型)
└── docker-compose*.yml        # 4 套 Docker 部署方案
```

---

## 二、模块职责详解

### 2.1 后端核心模块

#### 2.1.1 中间件层 (middlewares/)

| 中间件 | 文件 | 职责 | 问题 |
|--------|------|------|------|
| `authenticate` | auth.middleware.ts | JWT验证 + Token黑名单检查 | ✅ 安全 |
| `requireRole` | rbac.middleware.ts | 角色权限检查 (ADMIN/MANAGER/USER) | ⚠️ 见问题11 |
| `getTaskFilterByRole` | rbac.middleware.ts | 部门递归数据隔离 | ⚠️ N+1递归查询 |
| `auditLog` | audit.middleware.ts | POST/PUT/DELETE自动审计 | ✅ 合规 |
| `errorHandler` | error.middleware.ts | 全局错误处理 + AppError类 | ⚠️ 栈泄漏风险 |

#### 2.1.2 审查管线 (review-pipeline/) — 核心业务引擎

这是整个系统最复杂的模块，采用**策略模式 + 能力驱动**设计：

```
ReviewPipeline (接口)
  └─ BasePipeline (抽象基类)
       ├─ StandardPipeline    — 以库审文/一致性/自定义规则
       ├─ DocReviewPipeline   — 以文审文(参照比对)
       ├─ MultimodalPipeline  — 多模态(表格/公式/图纸)
       └─ TypoGrammarPipeline — 错别字/语法(术语过滤)
```

**两阶段编排流程：**
- **阶段1 (runFastPhase)**: 规则引擎(13条规则并行) + 标准引用检查 — 毫秒级
- **阶段2 (runSlowPhase)**: AI审查(RAG检索 → LLM) — 秒级，受用户级别并发控制

**六个审查模式的能力配置：**

| 模式 | rules | ai | aiStrategy | crossFile | needsRef |
|------|-------|----|-----------|-----------|----------|
| LIBRARY_REVIEW | ❌ | ✅ | standard | ❌ | ❌ |
| DOC_REVIEW | ❌ | ✅ | refCompare | ❌ | ✅ |
| CONSISTENCY | ❌ | ✅ | standard | ✅ | ❌ |
| TYPO_GRAMMAR | ❌ | ✅ | llmOnly | ❌ | ❌ |
| MULTIMODAL | ❌ | ✅ | multimodal | ❌ | ❌ |
| CUSTOM_RULE | ✅ | ❌ | standard | ❌ | ❌ |

**提取的四个独立服务模块：**
1. `TextExtractionService` — 文本提取 (Parser + OCR + Word结构 + DWG结构)
2. `AiReviewService` — AI审查 (RAG路由 / LLM-only / 多模态)
3. `StandardRefCheckService` — 标准引用8级比对 + 字符级差异
4. `pipeline-config.ts` — 配置工具函数

#### 2.1.3 并发控制 (双层级)

| 层级 | 实现 | 范围 |
|------|------|------|
| 用户级别 | `review.service.ts` 内存 Map | 单进程内，用户间公平 |
| 全局级别 | `ConcurrencyService` Redis 计数器 | 跨进程，全局上限 5 |

**问题**: 用户级别使用内存Map，多进程部署时失效（见问题5）。

#### 2.1.4 数据库层 (Prisma — 28 模型)

核心模型关系：
```
Department (树形自引用)
  └─ User (ADMIN/MANAGER/USER)
       └─ Task (关联 Standard/ReviewSpecification)
            ├─ TaskFile[] (关联 extractedText/extractedMarkdown/DWG元数据)
            │    └─ FileVersion[] (版本快照)
            └─ TaskDetail[] (审查结果, 含 locateMeta/textPosition)
                 └─ 误报标记 (isFalsePositive)

Standard (关联 StandardFolder 树)
  └─ TaskStandard (多对多)

KnowledgeCategory (知识子库树, 含权限)
  └─ Document (版本管理)
       └─ VectorDocument (pgvector 4096维嵌入)

ReviewSpecification (审查规范集)
  └─ ReviewSpecificationItem[] (规则项, 支持 BUILTIN_PREFIX/REGEX/KEYWORD/MANUAL)

PromptTemplate (提示词模板, 6种场景 × 7种变体)

Feedback / SystemAnnouncement / QASession (辅助功能)
```

### 2.2 前端核心模块

#### 2.2.1 状态管理 (Pinia)

仅 **1 个 Store** (`stores/user.ts`)：
- `token` / `userInfo` (持久化到 localStorage)
- `isAdmin()` / `isManager()` / `isUser()` 角色判断
- `login()` / `logout()` 方法

> ⚠️ **问题**: 单 Store 架构在功能增长后维护困难，审查状态（任务进度、WebSocket连接状态）散落在各组件中。

#### 2.2.2 API 层 (20 个模块)

使用 Axios 封装 (`utils/request.ts`)：
- 自动附加 JWT Bearer Token
- `{ code: 200, message, data }` 统一响应格式提取
- 401 → 自动登出跳转登录页
- 路由切换时统一取消 pending 请求 (`cancelAllPendingRequests()`)
- API 响应中 `data.data` → `response.data` 转换

#### 2.2.3 核心页面组件

| 页面 | 大小 | 职责 |
|------|------|------|
| `SmartReview.vue` | 88KB | 智能审查主流程入口 |
| `TaskResultsView.vue` | 81KB | 审查结果展示 (最复杂页面) |
| `Dashboard.vue` | ~30KB | 数据看板 (用户/领导双视图) |
| `TaskHistory.vue` | ~20KB | 任务历史列表 |

> ⚠️ **问题**: `SmartReview.vue` (88KB) 和 `TaskResultsView.vue` (81KB) 属于超大组件，建议拆分。

#### 2.2.4 关键工具模块

| 工具 | 文件 | 功能 |
|------|------|------|
| SSE 流式 | utils/sse.ts | Server-Sent Events 封装 |
| DWG WASM | utils/dwg-parser.ts | 前端 DWG 解析 (libredwg-web) |
| WebSocket | composables/useWebSocket.ts | 审查进度实时推送 |
| ECharts | composables/useECharts.ts | 图表实例管理 (自动resize) |
| 轮询 | composables/usePolling.ts | 基于 @vueuse/core |

### 2.3 辅助服务

#### 2.3.1 MarkItDown 文档解析服务

| 属性 | 值 |
|------|-----|
| 框架 | FastAPI + Uvicorn |
| 格式 | DOCX/XLSX/PDF/PPTX → Markdown + 结构化 |
| 增强策略 | PDF: PyMuPDF + pdfplumber / DOCX: python-docx / XLSX: openpyxl |
| API | `/api/parse` (主接口) / `/health` / `/api/convert` |

#### 2.3.2 OCR 服务

| 属性 | 值 |
|------|-----|
| 引擎 | PaddleOCR 3.5 + PPStructureV3 |
| 识别 | 中文文字识别 (use_angle_cls) |
| 增强 | 版式分析 (layout/table/formula) |
| Vision备选 | GPT-4V 等第三方API兜底 |
| 资源 | 4G内存 / 2G预留 / 2 CPU (最重服务) |

---

## 三、依赖关系

### 3.1 核心服务依赖图

```
controllers/
  ├─ task.controller.ts → TaskService → ReviewService
  │                         ├─ PipelineFactory
  │                         │    ├─ StandardPipeline
  │                         │    ├─ DocReviewPipeline
  │                         │    ├─ MultimodalPipeline
  │                         │    └─ TypoGrammarPipeline
  │                         │         └─ BasePipeline
  │                         │              ├─ TextExtractionService
  │                         │              │    ├─ ParserService → PythonParserService
  │                         │              │    └─ OcrService
  │                         │              ├─ AiReviewService → LlmService
  │                         │              └─ StandardRefCheckService
  │                         ├─ CrossFileConsistencyService
  │                         ├─ IntraFileConsistencyService
  │                         ├─ WebSocketService
  │                         └─ ConcurrencyService → Redis
  │
  ├─ knowledge-category.controller.ts → KnowledgeCategoryService
  │    └─ EmbeddingService → pgvector
  │
  ├─ langchain.controller.ts → LangChainRagService → pgvector
  │
  └─ auth.controller.ts → TokenService (JWT + Redis 黑名单)
```

### 3.2 数据库模型关联

```
Department ──< User ──< Task ──< TaskFile ──< TaskDetail
                │                    │
                │                    └── FileVersion
                │
                ├──< Feedback
                ├──< SystemAnnouncement ──< UserAnnouncementRead
                └──< QASession ──< QAMessage

StandardFolder ──< Standard ──< TaskStandard >── Task

KnowledgeCategory ──< Document ──< VectorDocument
       │               └── DocumentVersion
       ├──< Tag ──< DocumentTag
       └──< KnowledgeCategoryPermission

SpecificationFolder ──< ReviewSpecification ──< ReviewSpecificationItem

RuleFolder ──< RuleLibrary ──< RuleLibraryItem

PromptTemplate (独立)
FalsePositiveLibrary (独立)
TerminologyWhitelist (独立)
TempStandardEntry (独立)
```

### 3.3 前端组件依赖

```
App.vue
  └─ AppLayout.vue (侧边栏 + 顶栏 + 内容区)
       ├─ views/Login.vue
       ├─ views/Workspace.vue (工作台首页)
       ├─ views/Dashboard.vue (数据看板)
       ├─ views/SmartReviewNew.vue (审查入口)
       │    └─ views/SmartReview.vue (主审查流程, 88KB)
       │         └─ components/DwgPreview.vue
       ├─ views/TaskResultsView.vue (结果展示, 81KB)
       │    ├─ components/TextPreviewPanel.vue
       │    └─ components/DwgPreviewPanel.vue
       ├─ views/TaskHistory.vue (任务列表)
       ├─ views/PromptConfig.vue (提示词配置)
       ├─ views/SystemManagement.vue (系统管理)
       ├─ views/StandardLibrary/ (标准库)
       └─ views/TaskDetails/ (任务详情)
```

---

## 四、设计模式识别

### 4.1 已应用的设计模式

| 模式 | 应用位置 | 评价 |
|------|----------|------|
| **策略模式** | Pipeline (BasePipeline + 4子类) | ✅ 核心设计，优秀 |
| **工厂模式** | `PipelineFactory.createPipeline()` | ✅ 解耦创建逻辑 |
| **单例模式** | Prisma client (`config/db.ts`) | ✅ 标准实践 |
| **门面模式** | `ParserService` → `PythonParserService` | ✅ 隔离实现细节 |
| **观察者模式** | `WebSocketService.emitTaskProgress()` | ✅ 实时推送 |
| **能力驱动配置** | `ModeCapabilities` 声明式配置 | ✅ 从代码逻辑变为数据驱动 |
| **中间件链** | Express 中间件栈 | ✅ 标准实践 |
| **仓库模式** | Prisma ORM (隐式) | ✅ 类型安全 |
| **模板方法** | `BasePipeline.execute()` 定义骨架 | ✅ 子类覆盖钩子 |

### 4.2 可改进的设计

| 问题 | 位置 | 建议模式 |
|------|------|----------|
| 单 Store 架构 | stores/user.ts | 拆分为 userStore / taskStore / wsStore |
| 超大组件 | SmartReview.vue (88KB) | 组合模式拆分 |
| 服务类静态方法 | 所有 Service 类 | 可考虑依赖注入 (非必须) |
| 控制器直接调用 Prisma | 部分 controller | 应全部委托 Service |

---

## 五、问题诊断

### 5.1 🔴 高危问题 (需立即修复)

#### 问题1: 错误中间件栈泄漏
- **位置**: `backend/src/middlewares/error.middleware.ts:39`
- **代码**: `data: process.env.NODE_ENV === 'development' ? { stack: err.stack } : null`
- **风险**: 依赖 NODE_ENV 判断，若误设为 development 则泄漏内部路径
- **建议**: 默认不返回 stack，仅在显式开启 debug 模式时返回

#### 问题3: 审计中间件所有路由生效
- **位置**: `backend/src/app.ts:56` — `app.use(auditLog)` 在路由前全局注册
- **风险**: 每个 POST/PUT/DELETE 都触发审计日志写入，高并发时数据库压力大
- **建议**: 对高频接口（如 WebSocket 进度推送相关）排除审计

### 5.2 🟡 中危问题 (建议近期修复)

#### 问题4: RBAC 递归查询 N+1 问题
- **位置**: `backend/src/middlewares/rbac.middleware.ts:60-73`
- **代码**: `getSubDepartmentIds()` 递归逐层查询子部门
- **影响**: 部门层级深时，每次 MANAGER 查询触发 N 次 DB 查询
- **建议**: 使用递归 CTE 一次查询所有子部门 ID

```sql
-- 建议替换为:
WITH RECURSIVE dept_tree AS (
  SELECT id FROM departments WHERE id = $1
  UNION ALL
  SELECT d.id FROM departments d
  INNER JOIN dept_tree dt ON d.parent_id = dt.id
)
SELECT id FROM dept_tree;
```

#### 问题5: 用户级并发控制仅内存
- **位置**: `backend/src/services/review.service.ts` (processingCount Map)
- **影响**: 多进程/pm2 cluster 部署时，同一用户可能超额并发
- **建议**: 将用户级并发计数也迁移到 Redis

#### 问题6: 超大组件需拆分
- **位置**: `frontend/src/views/SmartReview.vue` (88KB) / `TaskResultsView.vue` (81KB)
- **影响**: 维护困难、编译慢、代码审查困难
- **建议**: 
  - SmartReview → 拆分为 StepFileUpload / ModeSelector / KnowledgeSelector / ReviewSettings
  - TaskResultsView → 拆分为 ResultSummary / IssueList / IssueDetail / DiffViewer / ExportPanel

#### 问题7: DWG WASM 主线程解析
- **位置**: `frontend/src/utils/dwg-parser.ts`
- **影响**: 大 DWG 文件解析阻塞 UI 线程
- **建议**: 已有 `dwg-worker.ts`，但需确认是否真正使用了 Web Worker

#### 问题8: 前端 console.log 残留
- **位置**: 61 处 `console.log/error/warn` (仅后端)
- **影响**: 生产环境日志污染
- **建议**: 统一使用日志框架 (winston/pino)，按级别输出

#### 问题9: 任务队列使用不一致
- **位置**: 
  - `controllers/knowledge-category.controller.ts:132` — 使用 `setImmediate()`
  - `services/task.service.ts` — 使用 `addReviewJob()` (Bull队列)
  - `services/review.service.ts` — 直接调用 processTask
- **影响**: 异步处理方式不统一，setImmediate 无持久化
- **建议**: 全部统一使用 Bull 队列

### 5.3 🟢 低危问题 (长期优化)

#### 问题10: 前端 TODO/FIXME 残留
- **位置**: `TaskResultsView.vue` (3处) / `NotificationCenter.vue` (1处)
- **内容**: 专项审查API、重审API、diff获取 等功能未完成
- **建议**: 根据产品规划决定实现或删除

#### 问题11: RBAC 中间件类型安全
- **位置**: `middlewares/rbac.middleware.ts`
- **问题**: `req.user` 类型为 `any`，`getTaskFilterByRole(user: any)`
- **建议**: 定义 AuthUser 接口替代 any

#### 问题12: Prisma Schema 缺乏迁移管理
- **位置**: `backend/prisma/` — 仅 1 个迁移文件
- **影响**: 无法追踪 schema 变更历史
- **建议**: 每次 schema 变更后运行 `prisma migrate dev --name xxx`

#### 问题13: 缺少 API 限流
- **位置**: 全局
- **影响**: 无 brute-force 保护，登录接口可被暴力破解
- **建议**: 添加 `express-rate-limit` 中间件，特别是 `/api/auth/login`

#### 问题14: 缓存策略缺失
- **位置**: `CacheService` 存在但使用情况有限
- **影响**: 高频查询（标准库列表、部门树）每次查 DB
- **建议**: 对读多写少的数据使用 Redis 缓存 + TTL

#### 问题15: 缺少 API 文档
- **位置**: 项目无 Swagger/OpenAPI
- **建议**: 考虑使用 `tsoa` 或手写 OpenAPI spec

#### 问题16: 前端单 Store 架构
- **位置**: `stores/` 仅 `user.ts`
- **影响**: 审查状态散落在各组件中，WebSocket 连接状态无统一管理
- **建议**: 新增 `taskStore` (审查进度) / `wsStore` (WebSocket状态)

#### 问题17: Docker 镜像体积大
- **位置**: OCR 服务使用 PaddleOCR (含模型文件)，镜像体积可能 5GB+
- **建议**: 模型文件挂载 volume，镜像仅含代码

---

## 六、优化建议路线图

### 第一阶段 (紧急 — 1周内)
1. ✅ 修复错误中间件栈泄漏
2. ✅ 修复 RBAC 递归查询为 CTE
4. ✅ 统一任务异步处理为 Bull 队列

### 第二阶段 (短期 — 2周内)
5. 用户级并发控制迁移到 Redis
6. 前端大组件拆分 (SmartReview / TaskResultsView)
7. 生产环境 console.log 清理 + 日志框架
8. 登录接口添加 rate-limit

### 第三阶段 (中期 — 1个月内)
9. DWG WASM 确认 Web Worker 实际使用
10. Prisma Schema 迁移规范化
11. 缓存策略优化 (标准库/部门树)
12. API 文档生成

### 第四阶段 (长期 — 2-3个月)
13. 前端 Store 拆分 (taskStore / wsStore)
14. 审计中间件性能优化 (异步批量写入)
15. OCR 镜像瘦身
16. 单元测试 / 集成测试体系建设

---

## 七、总结

### 7.1 架构优势
- **管线架构优秀**: 策略模式 + 能力驱动的 Pipeline 设计是项目最大亮点
- **两阶段并行编排**: 快速规则审查 + 慢速AI审查分离，实时进度推送
- **多层次降级**: auto → RAG → LLM+知识库 → LLM直接调用
- **DWG 双路径**: 前端 WASM + 后端 MarkItDown 协同工作
- **Prisma 类型安全**: 28 个模型完整定义，relation 关联清晰
- **审计合规**: POST/PUT/DELETE 自动审计日志
- **Docker 部署成熟**: 4 套 compose 方案 (开发/生产/离线/OCR)

### 7.2 核心风险
- 并发控制的内存依赖（多进程失效）
- 超大前端组件 (88KB+81KB) 维护性差
- 异步处理方式不统一 (setImmediate vs Bull vs 直接调用)
- 缺少测试体系

### 7.3 技术债务总量评估

| 类别 | 数量 | 严重度 |
|------|------|--------|
| 安全漏洞 | 2 | 🔴 高危 |
| 性能隐患 | 4 | 🟡 中危 |
| 代码不规范 | 6 | 🟢 低危 |
| 功能未完成 (TODO) | 4 | 🟢 低危 |
| 架构改进 | 4 | 🟢 低危 |

---

*报告生成完毕。建议按路线图分阶段推进，优先处理第一阶段的 4 个高危/中危问题。*
