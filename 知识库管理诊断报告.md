# 知识库管理功能 — 全面诊断报告

> 诊断日期：2026-05-23 | 审查范围：知识库 CRUD + 文档管理 + 标签系统 + 向量化

---

## 一、功能覆盖率矩阵

### 1.1 知识库 CRUD

| 功能 | 后端路由 | 控制器 | 服务层 | 前端调用 | 状态 |
|------|----------|--------|--------|----------|------|
| 创建知识库/文件夹 | `POST /` | `createCategory` | `KnowledgeCategoryService.create()` | `KnowledgeCategories.vue` | ✅ 正常 |
| 编辑知识库 | `PUT /:id` | `updateCategory` | `KnowledgeCategoryService.update()` | `KnowledgeCategories.vue` | ✅ 正常 |
| 删除知识库 | `DELETE /:id` | `deleteCategory` | `KnowledgeCategoryService.delete()` | `KnowledgeCategories.vue` | ✅ 正常 |
| 列表查询（树形） | `GET /` | `listCategories` | `list()` | `KnowledgeCategories.vue` | ✅ 正常 |
| 扁平列表 | `GET /all` | `listAllCategories` | `listAll()` | - | ⚠️ 前端未使用 |
| 树形查询 | `GET /tree` | `getTree` | `getKnowledgeTree()` | `KnowledgeCategories.vue` | ✅ 正常 |

### 1.2 文档管理

| 功能 | 后端路由 | 状态 | 问题 |
|------|----------|------|------|
| 分组文档列表 | `GET /:id/grouped-documents` | ✅ 已修复 | 原 DocumentTag 查询字段不存在 → 已修复 |
| 同步上传 | `POST /:id/documents` | ✅ 正常 | |
| 异步上传 (多文件) | `POST /:id/upload-async` | ⚠️ 部分 | `setImmediate` 无持久化 |
| 分段预览 | `POST /:id/preview` | ✅ 正常 | |
| 确认导入 | `POST /:id/confirm-import` | ⚠️ | 未创建 Document 记录 |
| 更新文档名 | `PUT /:id/documents` | 🔴 BUG | DocumentTag 字段不存在 |
| 删除文档 | `DELETE /:id/documents` | ✅ 正常 | |
| 文档段落查询 | `GET /:id/document-paragraphs` | ✅ 正常 | |
| 段落更新 | `PUT /paragraphs/:paragraphId` | ✅ 正常 | |
| 段落删除 | `DELETE /paragraphs/:paragraphId` | ✅ 正常 | |
| 批量向量化 | `POST /:id/batch-vectorize` | ✅ 正常 | |

### 1.3 标签系统

| 功能 | 后端路由 | 状态 | 问题 |
|------|----------|------|------|
| 标签 CRUD | `GET/POST/DELETE /tags` | ✅ 正常 | |
| 文档标签查询 | `GET /:id/document-tags` | 🔴 BUG | DocumentTag 字段不存在 |
| 添加文档标签 | `POST /:id/document-tags` | 🔴 BUG | DocumentTag 字段不存在 |
| 移除文档标签 | `DELETE /:id/document-tags` | 🔴 BUG | DocumentTag 字段不存在 |

---

## 二、🔴 致命 Bug

### Bug 1: `updateDocument` — DocumentTag 字段不存在

**位置**: `knowledge-category.controller.ts:469-472`

```typescript
// ❌ DocumentTag 模型没有 categoryId 和 documentTitle
await tx.documentTag.updateMany({
  where: { categoryId, documentTitle: oldTitle },
  data: { documentTitle: normalizedTitle },
});
```

**影响**: 更新文档名称时 500 错误，文档名更新后标签关联断裂。

**修复**: 通过 Document 表查 documentId，再更新 Document 表的 title + 重建 documentId 映射。

---

### Bug 2: `addDocumentTag` — DocumentTag 字段不存在

**位置**: `knowledge-category.controller.ts:698`

```typescript
const docTag = await prisma.documentTag.create({
  data: { tagId, documentTitle, categoryId },  // ❌ 无这些字段
});
```

**影响**: 给文档添加标签时 500 错误，标签功能完全不可用。

**修复**: 从 `documentTitle` 反查 `documentId`，再以 `documentId` 创建 DocumentTag。

---

### Bug 3: `removeDocumentTag` — DocumentTag 字段不存在

**位置**: `knowledge-category.controller.ts:714`

```typescript
await prisma.documentTag.deleteMany({
  where: { tagId, documentTitle, categoryId },  // ❌ 无这些字段
});
```

**影响**: 移除文档标签时 500。

---

### Bug 4: `getDocumentTags` — DocumentTag 字段不存在

**位置**: `knowledge-category.controller.ts:733`

```typescript
const docTags = await prisma.documentTag.findMany({
  where: { categoryId, documentTitle: title as string },
  //       ^^^^^^^^^^  ^^^^^^^^^^^^^^  两字段均不存在
});
```

**影响**: 查询文档标签列表时 500。

---

### Bug 汇总根因

**数据库 `DocumentTag` 模型缺少 `categoryId` 和 `documentTitle` 字段**。代码中多处直接以这两个字段做查询/写入，全部 500 失败。需同步修正：

- **方案 A (推荐)**：在 `DocumentTag` 模型中添加 `categoryId` 和 `documentTitle` 冗余字段（去范式），`prisma migrate` 建列 + 回填数据
- **方案 B**: 全改为 `documentId` 查询，每次先查 Document 表映射

---

## 三、🟡 数据完整性问题

### 3.1 `uploadDocument` 未创建 `Document` 记录

**位置**: `knowledge-category.service.ts:358-417`

调用 `VectorService.importDocument()` 只写入了 `VectorDocument` 表，但未创建对应的 `Document` 记录。导致：
- `Document` 表和 `VectorDocument` 表数据断层
- 文档级元数据（版本、状态、作者）缺失
- `listGroupedDocuments` 无法通过 `Document` 表建立文档层面的统计

**建议**: `uploadDocument` 增加 `Document.upsert` 步骤。

### 3.2 `confirmImport` 也未创建 `Document` 记录

**位置**: `knowledge-category.controller.ts:267`

```typescript
const result = await VectorService.importDocuments(entries);
```

同上，仅写 VectorDocument，缺 Document。

### 3.3 KnowledgeCategory.isLeaf 维护逻辑不完整

**位置**: `knowledge-category.service.ts`

- ✅ 创建子节点时标记父节点 `isLeaf: false`
- ✅ 删除最后一个子节点时恢复父节点 `isLeaf: true`
- ⚠️ `deleteCategory` 只检查了 `_count.children` 和 `_count.vectorDocuments`，未级联删除子节点
- ⚠️ 批量删除无事务保护

### 3.4 Document.parsedChunkCount / totalChars 未更新

上传文档后，`Document` 表不存在记录，自然 `totalChunks` / `totalChars` 等统计字段永远为 0。

---

## 四、🟡 架构与性能问题

### 4.1 上传任务内存存储 — 无持久化

**位置**: `upload-task.service.ts`

```typescript
const tasks = new Map<string, UploadTaskStatus>(); // 💀 进程内存
```

- 服务重启 → 所有进行中任务状态丢失
- 多进程部署 → 任务状态互相不可见
- **建议**: 迁移到 Redis 或 Bull 队列

### 4.2 `setImmediate` 异步处理 — 无失败重试

**位置**: `knowledge-category.controller.ts:132`

```typescript
setImmediate(async () => {
  // 上传处理...
});
```

- 无重试机制，失败即丢失
- 无并发上限控制（对比 `review.service.ts` 的 `ConcurrencyService`）
- **建议**: 统一使用 Bull 队列

### 4.3 `listGroupedDocuments` 多轮 SQL 查询

**位置**: `knowledge-category.controller.ts:369-412`

一次 API 调用需 4 轮查询：
1. `vector_documents` COUNT
2. `vector_documents` SELECT
3. `documents` findMany (按 title) → **新增**
4. `documentTags` findMany (按 documentId) → **新增**

**建议**: 第 3-4 步可合并为单次 JOIN SQL。

### 4.4 `fetchCategoryInfo` 全量加载树后递归查找

**位置**: `KnowledgeDocuments.vue:777-796`

```typescript
const { data } = await getKnowledgeCategoriesApi()  // 加载整棵树
const findById = (nodes, id) => { ... }              // 递归遍历
```

每次进入文档页面都加载整棵知识树，应使用 `GET /all` 或直接按 ID 查询。

### 4.5 轮询矢量更新状态 — 无上限

**位置**: `KnowledgeDocuments.vue:836-839`

```typescript
pollTimer = setInterval(() => { fetchDocuments() }, 6000)  // 每6秒
```

- 只要有 PENDING 文档就无限轮询
- 无最大轮询次数 / 总超时限制
- 用户离开页面时应清理（已有 `stopPolling` 但 `onUnmounted` 未确认调用）

---

## 五、🟢 边界情况与交互问题

### 5.1 空名/重名未处理

- 创建知识库时只校验了 `name?.trim()` 非空，未检查同名（Prisma 无 unique 约束可建同名）
- 文档上传时同名文件覆盖未提示

### 5.2 并发：同时上传同名文档

`uploadDocument` 和 `uploadDocumentAsync` 无并发锁，两个同时上传同名文件会产生两份 `VectorDocument` 记录（语义上应合并）。

### 5.3 删除知识库——硬删除且不可撤销

- `deleteCategory` 直接 `prisma.knowledgeCategory.delete()`
- 无软删除（status 字段有 ACTIVE/ARCHIVED 但未见回收站逻辑）

### 5.4 批量向量化——断点续传缺失

`batchVectorize` 逐文档处理，若中途失败：
- 已处理的文档正常
- 后续文档未处理
- 无整体事务或断点记录

### 5.5 分页不完整

`listGroupedDocuments` 分页正常，但 `listDocuments` 的 `VectorService.listDocuments` 需确认是否也有分页。

---

## 六、数据流转梳理

```
┌─────────────────────────────────────────────────┐
│               前端操作流程                         │
├──────────────┬──────────────────────────────────┤
│ KnowledgeCat │ 创建/编辑/删除 知识库              │
│ egories.vue  │ → /api/knowledge-categories       │
│              │    CRUD (控制器 → Service)         │
├──────────────┼──────────────────────────────────┤
│ KnowledgeDoc │ 进入文档管理                        │
│ uments.vue   │ → GET /:id/grouped-documents       │
│              │    (vector_documents 分组聚合)      │
│              │ → 上传: async/sync                 │
│              │ → 向量化/段落编辑/标签              │
└──────────────┴──────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              数据表关系（现状）                     │
├─────────────────────────────────────────────────┤
│ KnowledgeCategory (树形)                         │
│   ├── VectorDocument (title→chunks)   ← 🔗 有数据 │
│   ├── Document            ← 💀 空表               │
│   │   └── DocumentVersion  ← 💀 空表              │
│   ├── Tag                  ← ✅ CRUD 正常          │
│   └── DocumentTag          ← 🔴 Bug 无法读写       │
│       (缺 documentTitle/categoryId)               │
└─────────────────────────────────────────────────┘
```

**核心流程通顺但存在两个断层：**
1. `VectorDocument` ↔ `Document`：前者有数据，后者为空（uploadDocument 未创建）
2. `Tag` ↔ `DocumentTag`：标签 CRUD 正常，但关联表因字段缺失完全不可用

---

## 七、修复优先级

| 优先级 | 问题 | 影响范围 |
|--------|------|----------|
| 🔴 P0 | Bug 1-4: DocumentTag 缺字段 | 标签系统完全不可用，文档重命名 500 |
| 🔴 P0 | uploadDocument 未创建 Document 记录 | 文档级元数据全空 |
| 🟡 P1 | confirmImport 未创建 Document 记录 | 预览确认导入同样断层 |
| 🟡 P1 | setImmediate + 内存任务存储 | 异步上传无持久化、无重试 |
| 🟢 P2 | 轮询无上限 / 全量加载树 | 客户端性能 |
| 🟢 P2 | 硬删除无回收站 | 数据安全 |
| 🟢 P3 | 同名/并发/分页边界 | 健壮性 |

---

## 八、修复方案

### P0 修复：统一 DocumentTag 字段

**推荐方案 A — 在 Prisma Schema 中添加冗余字段：**

```prisma
model DocumentTag {
  id             String      @id @default(uuid())
  tagId          String      @map("tag_id")
  tag            Tag         @relation(fields: [tagId], references: [id], onDelete: Cascade)
  documentId     String      @map("document_id")
  document       Document    @relation(fields: [documentId], references: [id], onDelete: Cascade)
  
  // ↓ 新增冗余字段
  categoryId     String?     @map("category_id")      // 从 Document 冗余
  documentTitle  String?     @map("document_title")   // 从 Document 冗余
  
  createdAt      DateTime    @default(now()) @map("created_at")

  @@unique([tagId, documentId])
  @@index([documentId])
  @@index([categoryId])                               // 新增索引
  @@map("document_tags")
}
```

然后 `prisma db push` + 回填数据。

### P0 修复：uploadDocument 同步创建 Document

在 `KnowledgeCategoryService.uploadDocument()` 末尾增加：

```typescript
await prisma.document.upsert({
  where: { categoryId_title: { categoryId, title: fileName.replace(/\.\w+$/, '') } },
  update: { totalChunks: result.chunks, totalChars: content.length, isVectorized: true },
  create: {
    categoryId,
    title: fileName.replace(/\.\w+$/, ''),
    sourceType: 'standard',
    status: 'ACTIVE',
    totalChunks: result.chunks,
    totalChars: content.length,
    isVectorized: true,
  },
});
```

---

*报告完毕。建议按 P0 → P1 → P2 顺序修复。*
